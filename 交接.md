# 交接

> 项目总况 → `README.md`（本会话变更见 `README/11-实现记录.md` §16.24–§16.28）；待办 → `TODO.md`；世界观 → `trpg-world/`。
> 本会话主题：**任务系统收尾 → 画作系统 → 双GM 拆分（叙事GM/工具GM）→ 选曲条件化**。

---

## ★ 本会话总结（2026-09-26）— 画作 / 双GM 拆分 / 选曲条件化 / 一批修复

> 分节详见 `README/11-实现记录.md`：§16.24（任务判定/UI/远端路由/脱离卡死/GM可配置）、§16.25（画作+后端推送）、§16.26（双GM）、§16.27（选曲条件化）、§16.28（零散修复）。
> 两份设计文档：`trpg-server/文档/设计-画作与后端推送.md`、`trpg-server/文档/设计-双GM拆分.md`。

### 一、⚠️ 上手必读

1. **后端必须重启一次**（本会话改了**一大批 `.py`**）：
   `llm.py` `main.py` `engine.py` `save_pipeline.py`、`tools/核心/{quest,instructions,map_query,ui_events,art,push,tool_gm,two_gm}.py`、`tools/大模型/{registry,quest_arbiter,location,facility}.py`、`tools/小模型/{quest_sim,ui_sim}.py` `tools/小模型/remote_model.py`。
   **只跑一个 `python main.py`**。
2. **前端硬刷新**：任务 HUD/任务页、`/state` 异步事件、菜单新按钮（任务/脱离卡死）、画作浮层、右栏「执行日志」、音乐目录重组。
3. **热改免重启**：`配置/*.json`（含新增 `双GM.json` `画作.json` `小模型.json` `大模型.json`）、`trpg-world/音乐.json`、`trpg-world/主持人/*.md`。
4. **不要跑 `npm run build`**；改 TS 只跑 `npx tsc -b`。

### 二、本会话做了什么（按主题）

**A. 任务系统收尾（§16.24 / §16.28）**
- 修 UI 不刷新**根因**：`applyState` 漏消费 `/state` 的 `_ui_events`；任务页打开强制 refetch；HUD 改「未完成优先」。
- 里程碑判定：`quest_sim` 喂 `描述/真相/结局`；`quest._match_milestone` 支持序号；`quest.py` 加 `RLock` 修并发丢更新。
- 判定**路由远端**（`remote_model` + `配置/小模型.json`，现为 gpt-6-luna）且**异步**（后台 FIFO）。
- **脱离卡死**：`POST /unstick`（吸附最近可行走点）+ 菜单按钮。
- **GM 可配置**：`llm.py` 读 `配置/大模型.json`（热切 DeepSeek / gpt-6-luna）；`llm.py` 支持 `reasoning_effort`。**GM 仍用 DeepSeek**（luna 慢 ~2.8 倍）。

**B. 画作系统 + 后端主动推送（§16.25）**
- GM 工具 `commission_painting`（异步）→ `gpt-image-2.5` 生成 → `trpg-server/画作/<id>.png` → **后端主动发起 GM 回合**（`run_push`）呈现 → 前端浮层（对话框正上方、可关、不挡对话）。
- **叙事可异步**：新增 `tools/核心/push.py` 队列 + 前端 `GET /pending` 轮询。
- **画作台账常驻 GM**（`art.context_view()`，含可选 `寓意`）；**不做「解读」**（玩家自己读画）。

**C. 双GM 拆分（§16.26）— 本会话最大重构**
- **叙事GM（DeepSeek，4 只读工具）**：只叙事，**不碰状态**。
- **工具GM（luna，17 写工具，异步）**：**读整段叙事**自己判断并落地；写 `sessions/工具日志.jsonl` + 推 `kind:"toollog"` → 前端右栏日志。
- 纪律：工具GM **只给写工具**（查询类两边都不给）；未落地叙事注入 DS 防重复；`_clear_async_queues()` 在放弃/回滚前停掉所有异步源。
- 前端 `ToolLogPanel`：**默认折叠**、只显示有实际调用的轮次。
- **开关 `配置/双GM.json`（默认关）** → 关着就是旧单GM，行为不变。
- 实测：**DS 必须开思考**（关思考会漏写/空输出）。

**D. 选曲条件化（§16.27）**
- 小模型**退出叙事选曲** → 代码按 `trpg-world/音乐.json` 的**条件**判定（地点/时辰/追踪/人物/天气/日期/农历/节日/好感度/小游戏）。
- 计分＝加权和（`小游戏`=10，其余每键=1）→ 并列按清单顺序 → 全不中 → `默认`。
- 音乐目录重组：`动态/` → **`叙事探索/` + `战斗/` + `固定/`**。

### 三、当前分工一览

| 角色 | 模型 / 归属 | 配置 |
|---|---|---|
| GM 叙事 | DeepSeek `deepseek-v4-flash`（**开思考**） | `配置/大模型.json` |
| 工具GM（双GM 开启时） | `gpt-6-luna`（开思考，失败回退 DeepSeek） | `配置/双GM.json` |
| 任务里程碑判定 | `gpt-6-luna`（异步） | `配置/小模型.json` |
| 背景 `bg` | 本地 4B（`ui_sim`） | — |
| 音乐 `music` | **代码条件判定**（不问模型） | `trpg-world/音乐.json` |
| 表情 | 本地 4B（`expression_sim`） | — |
| 画作 | `gpt-image-2.5` | `配置/画作.json` |

### 四、待验证 / 遗留

- **双GM 尚未真机充分验证**：开 `配置/双GM.json` 的 `启用=true` 玩几轮，看叙事速度 / 日志 / 时间 / 重复扣钱。
- 🅿️ **异步清理缺口（搁置）**：`art`（一委托一线程）与 `quest_arbiter` 一次性线程**无法 clear**，在途线程可能在回滚后写 PNG/`画作.json`/`任务.json`。需可取消/世代号机制。见 `TODO.md`。
- **日历系统**未接入：`农历`/`节日` 条件恒不中；小游戏系统未接入：`小游戏` 条件恒不中。
- 门禁（时间/位置/饥饿/天气/金钱只扣一次）**未迁到工具GM**（双GM 下旧门禁被跳过）。
- 画作测试残留：`游戏数据/画作.json` 的「测试画师·桂花」+ `trpg-server/画作/p_3bd47cb6.png`（可删）。
- 杂项：`trpg-client/public/data/test.py` 疑似误放；`游戏数据/曲牌.json` 已删（曲牌退役）。

### 五、git 状态

本会话改动**全部未提交**（85 项：后端 .py / 前端 tsx+css / 地图 geojson / 大批 md / 新增文件 / chroma）。建议按主题拆提交：
① 任务判定+UI+并发锁；② 脱离卡死+菜单；③ GM 可配置；④ 画作+后端推送；⑤ 双GM 拆分；⑥ 选曲条件化；⑦ 文档。

---

# 上一轮交接原文 — 2026-09-26（任务系统 + 保活 + 好感度 + 位置/地图修复 + 上下文精简）

> 上一轮（2026-09-25）：背景系统重构 + 美术全量 + 仓库瘦身（§16.22）。

---

## 〇、一句话

新增**任务系统**（旁路大模型建/裁定 + 小模型报里程碑 + 代码结算，含 DDL）与**保活机制**（在场 NPC 档案常驻）；
**好感度**并入角色档案、存档结算；修掉**位置权威**（叙述移动却没落库）、**地点判错**（蜀冈被标成粮行）、
**画舫背景错**、**护城河/湖没桥**；精简 GM 上下文；后端文件重组（`配置/ 文档/ 开发/`、`tools/{战斗,服务}`）。

---

## 一、⚠️ 上手必读（本轮头等要务）

1. **后端必须重启一次**——本轮改了一大批 `.py`（`engine.py` / `main.py` / `quest*.py` / `location.py` /
   `ui_sim.py` / `facility.py` / `character_archive.py` / `registry.py` / `save_pipeline.py` / `ui_events.py` / `time_flow.py`）。
   **只跑一个** `python main.py`（本轮实测出现过两个实例）。
   > 已发生的一次“改了没生效”：`engine.py` 22:01 改、后端进程 21:47 起 → 位置门禁没加载，船到了位置还是没动。
2. **前端硬刷新**：任务按钮/追踪条、以及新的 `walkable.geojson`（过河桥）都要重新加载。
   若白屏，**重启 `npm run dev`**（Vite 会缓存空模块），浏览器硬刷新没用。
3. 热改免重启：`主持人/*.md`、`配置/*.json`、`trpg-world/场景表.json`、模板 md。
4. **不要跑 `npm run build`**；改 TS 只跑 `npx tsc -b`。

---

## 二、后端文件重组

- 根目录 15 → 4：只留 `main.py engine.py llm.py save_pipeline.py`。
- 新增 `配置/`（8 个 json）、`文档/`（删档.md、基础设施活动表.md、设计-任务系统与保活机制.md）、`开发/`（诊断脚本）。
- `context_dump.py` → `tools/核心/`（`llm.py` 的运行时依赖）。
- `tools/` 由 4 包 → 6 包：新增 **`战斗/`**（`battle*` 6 件）、**`服务/`**（recap/factions/difficulty_settings/place_recall）。
- 全量 import/路径同步 + `README/02` 结构树更新。**`游戏数据/任务.json` 之类未动。**

---

## 三、导演（GLM）

- `_save` 改为**只有给了新内容才覆盖**（旧实现「生成中」就把内容清空，失败必丢上一版）。
- 新增 `recover_stuck()`：续玩时若残留「生成中」→ 收尾或重跑。
- `refresh_async` 改「**最新请求待跑**」队列（存档/放弃撞上生成时，新局材料排队，不再被丢弃）。
- 注入只给 `内容`（不再把 状态/模型/生成于 等元数据塞进 GM 上下文）。
- 探针 `开发/director_probe.py`（既有）。

---

## 四、好感度（存档结算，**不建关系.json**）

- **并入角色档案**：好感度 → **动态档案 §9** 一行 `- **好感度**：N（档位）`；立场（所属/主张/底线/偏好/反感）→ **静态档案 §8.5**。
- `tools/大模型/character_archive.py`：`update_character_archive(..., 好感度变化=N)`（**±10 上限**、clamp -100~100、档位派生）；
  新增 `read_affinity(name)` / `affinity_band()`。
- 模板：`档案模板/动态模板.md` §9 加好感度行；`档案模板/静态模板.md` 加 §8.5。
- 存档规则 `trpg-world/存档流程.md`：蒸馏步骤 2 加 `好感度变化`；`save_pipeline.save_hints` 新增第三节**点名**本局互动过的人物。
- **GM 不给好感度工具**（只读；经**保活注入** / `get_character` 拿到档案即含好感度）。
- **不建小模型 `relation_sim`**（能力探针见 `开发/verify_relation.py`，结论：方向可靠、幅度不准）。
- 端到端探针 `开发/verify_save_affinity.py`（真 DeepSeek，2/2 合规：帮人 +6、威胁 -5）。

---

## 五、保活机制（在场档案常驻）

- `engine.py`：`_speakers_of` / `keepalive_block`；`GameSession.recent_speakers`（`deque(maxlen=3)`，只认 `chat` 发言人）。
- `build_messages`：在**状态块之后**注入近 3 轮开口者**整份档案**（静态+动态，含好感度）。
- 重启从 `current.jsonl` 最近 3 轮重建；`reset/abandon` 清空；`reject` 快照/回滚带上。
- 实测依据（`开发/verify_get_character.py`）：GM 每次都会调 `get_character`、且下轮不记得调过 → 保活省掉重复抓取。

---

## 六、任务系统（后端 + 前端）

### 数据
- `游戏数据/任务.json`：**运行时真相源，初始任务也直接写在这里**。字段：`id/标题/状态/可见/描述/委托人/里程碑[{项,状态}]/时限{类型,游戏秒,显示,文本}/失败后果/真相/结局/知情者/揭示节奏/扩展次数/已告知`。
- `配置/任务.json`：`奖励.技能点`、`里程碑扩展上限`（默认 3）。
- **弃用 `任务/种子.json`**：删掉 `ensure_seeded`，改 `quest.materialize_deadlines()`——手写任务可用相对 `时限时辰`，开局按当前时钟换算成绝对游戏秒（幂等）。
- **不分主线/支线**（无 `类型` 字段）；**不拆子任务**（大目标 = 一条任务 + 若干里程碑；里程碑**并列**）。

### 三个角色
- **旁路大模型** `tools/大模型/quest_arbiter.py`（DeepSeek，异步）：
  - `add`：玩家点「任务化」→ 读会话 → `add_quest`（**建任务那刻定死 真相/结局/知情者/揭示节奏**）。
  - **里程碑裁定**：里程碑全完成 → 判「补新里程碑 / 算真正完成」（喂 `真相`+`预定结局`，回一句**玩家可见的 `结果`**）。
  - **DDL 过期**：到时限 → 给结局 → 归档。带 in-flight 去重、扩展上限、失败机械了结。
- **小模型** `tools/小模型/quest_sim.py`：**只对「追踪」的任务**，报「本轮做到了哪些里程碑」（并列、可多条）→ 应用到 `任务.json` + 推 `kind:"quest"` 事件；返回**待裁定**任务。
- **代码** `tools/核心/quest.py`：增/改/结/过期/视图/时限；完成**静默发技能点**（GM 不知情）；过期 → 归档 + `pending_notes`（下一轮以「世界变动 / 事情了结」注入 GM 一次）。

### 真相（跟任务绑定）
- `真相/结局/知情者/揭示节奏` **仅 GM 可见**；**只注入玩家追踪的进行中任务**（`quest.truth_view(tracked_ids)`）；
- **玩家视图不含**（`GET /quests` 只回 标题/描述/里程碑/时限/结果）。
- ⚠️ `知情者` 目前只**存下来**，**未接**「信息边界」（NPC 开口只能引用自己知道的）——下一步。

### 接口 / 事件
- `GET /quests`（进行中 + 已了结，玩家投影）；`POST /quest/add`（玩家「任务化」）。
- `/action`：读 `追踪` → `session.tracked_ids`（决定注入哪些真相）+ 跑 `quest_sim` + 触发里程碑裁定；`/state` 剔除「任务」并附 `_ui_events`。
- UI 事件 kind **`quest`**（新增/推进/完成/续接/过期）；异步裁定的事件走 **`ui_events.push` 队列**，下一次 `/action` 或 `/state` 由 `drain()` 携回。
- `time_flow.pump()` 里查 DDL → `arbitrate_expiry_async`。

### 前端
- 底部按钮栏新增 **「任务」**；任务页复用**数据页面的宣纸 + 揭幕**（同一个 `StaggeredMenu` + `sm-data-inner`）。
- 每条进行中任务右上角**正方形勾选框**（打勾、**最多 3 个**）；页顶「**任务化当前事件**」。
- 追踪中的任务渲染在**右上角（与古钟平齐 top:70px）**：任务名大字 + ≤3 条里程碑（完成打勾灰掉）；点击打开任务页。
- 追踪偏好存 **localStorage**（`trpg.tracked.quests`）；每次 `/action` 带 `追踪`；收到 `kind:"quest"` 刷新任务栏。
- 文件：`src/in-game/QuestPanel.tsx` + `QuestTracker.tsx` + `styles/QuestPanel.css` + `QuestTracker.css`；`GameController.tsx` 接入 9 处；`GameController.css` 加 `.quest-button`。

### 设计文档
- `trpg-server/文档/设计-任务系统与保活机制.md`（含「已完成/未做」状态、角色分工、落地物清单）。

---

## 七、位置权威（修「船到了、位置没动」）

- `engine.py` 新增：
  - `_LOCATION_MOVE_RE` / `_location_moved`：识别叙述里的到达/移动（上岸|下船|靠岸|抵达|渡过|到了[。]|船…贴|埠头…近了…）。
  - `_location_guard`（**同轮硬门禁**）：行动回合叙述了移动却没 `update_location` → 当场再问 GM 一次并**直接执行它返回的 `update_location`**（位置即刻落库）。
  - `_check_location_authority` + `GameSession.pending_location_note`：台词/场外/观察回合（**不允许改位置**）→ 下一轮提醒 GM **别把梁峰当成已到达**。
- `主持人/总览.md` 规则 9 加一条（热改）：位置必须**同轮落库**；台词/场外/观察不得叙述「已到新地方」。
- ⚠️ 是**启发式**（靠到达词）；极端措辞可能漏（还有下一轮提醒兜底）。要 100% 机械 → 把「乘船」做成独立工具（按渡口自动找对岸码头、扣钱、推时间、移动）。

---

## 八、地点判错（蜀冈中峰被标成「务和粮行」）

- `tools/大模型/location.py` 名字回填 `_rank`：原 `(broad, distance, size)` 把「大区域」压到最底 → **任何小 POI 都压过最近的大区域**。
- 改 `(round(dist,2), broad, size)` —— **距离优先**，距离相近才让「具体地点」优先于大区域（治「站在锦香宫院里被标君山岛」的原始诉求不受影响）。
- 影响面广：地点 / 城区方位 / 背景 / 音乐 / 附近地点全都对。

---

## 九、画舫「详细」背景错（显示成码头）

- `tools/小模型/ui_sim.py`：`scene_for/scenes_for_kind/_scenes_for_kind` 加 `strict` 开关；`_eligible_scene` 改为**城外也允许「室内」场景**。
- `tools/大模型/facility.py`：`_scene_for` 用 `strict=False`（设施详细页**不按玩家位置过滤**，直接用该 kind 自己的场景）。
- 验证：画舫→画舫、粮行→粮行、山→山、青楼→青楼、棋馆→棋馆。

---

## 十、护城河 / 湖没桥（走不进扬州城）

- 实测：从蜀冈到**任何**城门最近 467m 且**不可达**（护城河一圈无桥 + 瘦西湖宽约 750m）。
- `trpg-map/draw_tiles/export_walkable.py` 自动补两类桥点：
  1. **城门护城河桥**：每个城门沿「城心→城门」方向往外放 2 点（40m/85m）；
  2. **路∩水桥**：道路与水面的相交段，沿顶点（长段补中点）补点。
- 重导两城 `walkable.geojson`：扬州桥点 **14→257**、岳阳 **6→74**。
- 验证：BFS 从蜀冈 → **北门可达（21m）**（改前 493m 不可达）。
- ⚠️ 东/南/西几门仍不可达（那几侧没检测到路∩水交叉）；城门仍**不能走着穿墙**（设计如此，走城门的「出城/入城」）。

---

## 十一、上下文精简（GM 每轮）

- `state_dict()`：跳过 **`任务` / `曲牌`**；`_strip_private` 去掉顶层 `_` 开头字段（`_备注`）；**空值**不注入。
- `stable_state_text()`：难度设置只投影**当前档**；地图设置只留 `地图`。
- **删掉「曲牌 / 篇名」块** + `_record_titles`（曲牌.json 退役）。
- **删掉「当前任务」块**（GM 不看任务列表）。
- 效果（对 `sessions/context_dump/0002-0925-200403.json`）：正文 18386 → 约 17300（≈ -6%，大头仍是规则 14428）。

---

## 十二、演示档重置

- 重置到 **扬州·蜀冈中峰**（`119.407477, 32.422021`），时间 `1220-01-17 申时初`，其余全部初始化；
  初始任务「投亲」（含 真相/结局/知情者/揭示节奏）。
- 备份：`trpg-server/删档备份/2026-09-25_演示档/`。
- 可复用脚本：`trpg-server/开发/重置演示档.py`（改开头 `LON/LAT/DATE` 可换开局点）。
- ⚠️ chroma **未清**（按约定不动「史书」）。

---

## 十三、探针 / 脚本（`trpg-server/开发/`）

| 脚本 | 作用 |
|---|---|
| `verify_get_character.py` | GM 是否登场就查档案（真 DeepSeek） |
| `verify_relation.py` | 小模型判好感度能力（方向 100%、幅度 ~65%、误动 0 需两段式） |
| `verify_quest.py` | 小模型做 add/update/complete（add 7/7、update/complete ~75-83%、**0 误判**） |
| `verify_save_affinity.py` | 存档蒸馏给不给 `好感度变化`（真 DeepSeek，2/2） |
| `重置演示档.py` | 重置演示档 |
| `verify_expression.py` / `verify_scene_table.py` / `context_lens.py` / `director_probe.py` / `导出策划数值表.py` | 既有 |

---

## 十四、待办 / 待拍板（详见 `TODO.md`）

1. **重启后端**（本轮所有 .py）+ 前端硬刷新。前端任务系统**尚未真机验证**。
2. **乘船机械工具**（位置门禁的终极解）。
3. **信息边界**：`知情者` 已入库，但未接「NPC 只能引用自己知道的」。
4. **曲牌机制退役**后，README/§16.21/交接 里旧表述待清。
5. 里程碑可挂**机器条件**（地点/物品）→ 代码自判，少问小模型。
6. 知识库层 / 环境事件层 / 战斗时间折算（长期）。

---

## 十五、git 状态

本轮改动**未提交**（后端 .py、前端 tsx/css、地图 geojson、大量 md）。建议按主题拆提交：
① 后端文件重组；② 导演修复；③ 好感度；④ 保活；⑤ 任务系统（后端）；⑥ 任务系统（前端）；
⑦ 位置/地点/画舫修复；⑧ 地图过河桥；⑨ 上下文精简；⑩ 文档。
