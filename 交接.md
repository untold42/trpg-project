# 交接 — 2026-09-18 增量（世界设定 + 上下文瘦身）

> **本文件只讲"这一轮改了什么、现在什么状态、下一步"。**
> 项目总况 / 架构 / 状态 → `README.md`；待办 → `TODO.md`；
> 世界观（导演用）→ `trpg-world/故事大纲.md`；GM 的世界设定 → `trpg-world/主持人/`。
>
> **⚠️ 本轮 53 个文件改动，全部未提交 git。**

---

## 〇、一句话

这一轮做了两件事：**① 把"五行/伪神/五支组织"这套新世界观落成正典**；
**② 修了一批工具边界 bug，并把 GM 的常驻上下文瘦了约 20%**（29,450 → 23,625 字/轮）。

---

## 一、最重要的架构决定：**信息分层（GM / 导演）**

```
导演（GLM）  看 → 故事大纲.md（主线/身份/结局/主题，含"谁不知道什么"）
   │  产出《导演简报》（导演简报.json）
   ▼
GM（DeepSeek）看 → 世界设定（主持人/*.md）+ 状态现拼 + 导演简报
```

- **GM 永远看不到 `故事大纲.md`**（它不在任何读取路径）。
- **GM 需要的世界设定** = `trpg-world/主持人/*.md`（全量注入）。
- 目前 **导演还没接大纲**——`tools/director.py` 仍只读"上一局存档"。**这是下一步第一件事。**

### GM 每轮实际看什么（`engine.build_messages()`）

| 块 | 来源 | 大小 |
|---|---|---:|
| 规则层 | `主持人/*.md`（3 个：世界/总览/可能性骰），注入前剥 markdown | 11,632 |
| 工具 schema | `ALL_TOOLS`（24 个） | 8,651 |
| 状态块 **×2**（上一轮 + 当前） | `游戏数据/*.json` 现拼 | 3,342 |
| 前情 / history / 附近地点 / 系统提醒 | — | 可变 |
| **常驻合计** | | **≈ 23,625 字** |

**状态块为什么两份**：`state_stack`（`deque(maxlen=2)`）——让模型自己对比"变了什么"，代码不算差值。

---

## 二、世界设定（新增 / 改写）

### 新增文件（**未跟踪**）

| 文件 | 用途 |
|---|---|
| `trpg-world/故事大纲.md` | **导演专用**（含剧透）。五行/五行神/伪神/五支/江湖三层/瑞杏多周目/梁峰五神共选/弑神者/多结局 |
| `trpg-world/世界设定·读本.md` | 手机阅读版（给人读） |
| `trpg-world/天气系统.md` | 从 `主持人/` **移出**（不再是常驻规则，改为设计文档） |
| `trpg-server/tools/character_expressions.py` | 由 `find_specific_expression.py` **改名**（旧名误导；只留 `CHARACTER_EXPRESSIONS` 表） |

### 世界观的**已定要点**（详见 `故事大纲.md`）

- **五行普世**：人人可觉醒，但一人一生**只能一行**；表现为**某一行内力**（可催可不催 → **看不出谁是觉醒者**）。
- **觉醒**：一次性；由**现实压迫 + 内心斗争**而来。**熄火 = 失去方向、随波逐流**（善/恶都是方向，只有随波逐流会熄）。
- **五行神（五尊）**：金**蓐收** / 木**句芒** / 水**玄冥** / 火**祝融** / 土**后土**。
  **从有资质的人里挑**（资质 = 精神 + 能力，各神偏好不同，见 `世界.md` 的表）；**神力有限**。
- **神不辨善恶，只辨"有没有方向"**——**连未来的弑神者也可能被授予**（一筛就变成伪神）。
- **伪神**：不是一个存在，是**由众人意识 + 上层建筑构成的结构**（人自愿走进的牢笼）。**与五行神没有对错，只有立场。**
- **拜神**：平民拜无用（神不看香火）；觉醒者可短暂沟通。
- **江湖三层**（离神/离伪神的远近）：五平台（觉醒多）→ 六大派+唐门（寥寥几人）→ **南宫·上官**（零觉醒）。
- **五支同源组织**（各生于一种压迫，**互不联系**）：泥教（瘟疫）/ 锦香宫（重男轻女）/ 白鲨帮（倭寇）/ 鬼门关（司法不公）/ 丐帮（贫富悬殊）。
- **梁峰** = 玩家本人；**五行神共选之子**，全属性觉醒但初始全 0；**对标瑞杏**（多周目者/幸福牢笼）。
- **弑神者** = 千灯楼 / 极乐教（"弑神" = 灭绝所有觉醒者）。

### `主持人/世界.md` 补了什么

**前置**了两节（不动原有 17 节编号，+1,098 字）：
`# 五行（世界的超自然力量）`（力量/五行神/拜神/伪神）、`# 江湖三层（离神/离伪神的远近）`。

---

## 三、正典修订（旧设定 → 新设定）

| 项 | 改动 |
|---|---|
| **泥教** | **拆成五支**：`泥教` = 原天道（车轩辕）；其余各自独立：锦香宫 / 白鲨帮 / 鬼门关（原地狱道）/ 丐帮。**旧的分支名号（"X道"）全项目已清除**（只剩地名"六道峡"） |
| **唐门** | 归**中间层**（不再算"最接近伪神"）；**不改**"与朝廷作对"。唐铮改为**纯唐门人**（删卧底/极乐右使身世） |
| **人物档案** | 温夫人/刘鄂/王二壮/赵逵/车轩辕（新结构）、瑞笙（去掉"红颜知己"清单）、梁有诗/孟仙谣/南溪/夏灵犀（**解除与瑞笙关系，独立**）、画中仙/马富贵（引用修正） |
| **觉醒名单** | **非觉醒者**：上官世家、南宫世家、瑞杏一系、朝廷（赵扩、宋悲）、唐门（唐升、唐惟元）。其余皆觉醒（六大派**平均每派约 3 人**，是"密度"） |
| **天气数据** | 东部 **+20 天雪**、南部 **+5 天雪**（离散、不连片） |
| **玩家可见** | `势力介绍.json` 同步（泥教改成五支同源；锦香宫去掉剧透句） |

---

## 四、代码改动

### 4.1 工具边界（**修了真 bug**）

- **`TurnRunner._call_tool` 加按轮白名单**：本轮没下发的工具**一律拒绝执行**。
  （原来 `TOOLS_MAP` 会把存档专用工具也放行，模型幻觉调用就能写库。）
- **`DB_add_and_update_tool` / `DB_query_tool_in_saving` 移入 `_SAVE_ONLY`**：**局内只读、不写记忆**（README §5.2）。
- **删 `check_expression`**（死代码；表情早由小模型 `expression_sim` 填）。

### 4.2 上下文瘦身（每轮）

| 项 | 省 |
|---|---:|
| 2 个 DB 工具移出游戏 | −1,234 |
| markdown 注入前剥离（`folder_to_prompt.strip_markdown` + `llm._plain_tools`） | −866 |
| `modify_money.reason` 删除 | −114 |
| `modify_injury` 全链路删除（伤势=HP 的派生，冗余） | −249 |
| **`属性.json` 移出每轮**（改由 `get_ability` 按需查） | **−3,512** |
| 天气系统（规则 + 工具） | −1,420 |
| 时间系统（规则/描述去重） | −429 |
| 地图工具描述去"扬州" | −44 |
| **加回**：`世界.md` 五行节、`get_ability` 描述 | **+1,146** |
| **净** | **≈ −5,800 字/轮（−20%）** |

- **`金钱` / `背包` 试过移出，又回退了**（收益仅 ~250 字，风险高于收益）。
- **`属性` 仍在外面**（`get_ability` 按需），工具描述已注明。

### 4.3 天气系统重构（**纯代码，LLM 出局**）

- **分区改按经纬度**（`WEATHER_ZONES`，4 个盒子 + 优先级）——不再靠城市名模糊匹配。
  验算：扬州/临安/南京/泉州→东部，岳阳/长沙→南部，成都→西部，开封→北部。
- **`weather_system.ensure_today()` 惰性同步**：`engine.state_dict()` 每轮调，**日期变了就重查** → GM 不用管天气。
- **`天气` 加代码算好的 `影响` 字段**（"轻功+5·火系减半"…）→ GM 直接引用。
- **删 `get_weather` 工具**；`update_weather` 保留（法术改天）。
- `天气系统.md` 移出 `主持人/`（不再常驻）。

### 4.4 时间系统

- `总览.md` 规则 8 / `update_time` / `sleep` 的**力学收敛到一处**（原来散在 4 处）。
- `_check_time_authority` 正则**补了"时辰级"**（过了两个时辰 / 两个时辰之后），且**不误报**"约两个时辰的路程"这类纯描述。

### 4.5 删掉的东西

`混乱度.json`（连同"混乱值"系统）、`modify_injury`、`check_expression`、`modify_money.reason`、
`get_weather` 工具、`主持人/天气系统.md`（移位）。

---

## 五、⚠️ 上手须知

1. **后端改了代码必须重启** `python main.py`；改规则 md / `时间影响.json` / `营业时间.json` 免重启。
2. **只跑一个后端**；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
3. **前端用 `npm run dev`，不要 `npm run build`**；改完 TS/TSX 只跑 `npx tsc -b`。
   `tsc` 抓不到 hooks 顺序等运行时错——**改完必须硬刷新看一眼**。
4. 地图命令**全部城市化**：单步跑脚本要带 `TRPG_CITY=<城市>`。
5. **GM 的常驻 = `主持人/`（全量）+ `游戏数据/*.json`（扫描即注入）**。
   → **想喂 GM 就放 `主持人/`；想藏起来就别放（或放 `trpg-world/` 根目录）。**
   → **千万别把剧透/剧情文件丢进 `游戏数据/`**（会每轮自动进上下文）。
6. 本轮 git **未提交**（53 项）。建议按主题拆提交：
   ① 世界观（故事大纲/读本/世界.md/泥教五支/角色档案）② 工具边界 ③ 上下文瘦身 ④ 天气 ⑤ 时间 ⑥ 地图描述。

---

## 六、下一步（按优先级）

1. **给导演接上 `故事大纲.md`**（改 `tools/director.py`：把大纲作为 system 材料一起喂）。
2. **定"第一场戏"（开场钩子）**——导演实测也指出"**最缺一个把玩家拉进故事的具体钩子**"。
   候选：五行剑被夺 / 瑞杏注意到棋盘外的人 / 一个熄火的人摔在他面前。
3. **`世界.md` 拆分**（用户说**先不移**）：社会结构/经济/日常/法律/信仰/男女（≈1,830 字）可移出常驻。
4. **开放设定问题**（见 `故事大纲.md` §八/§十）：五行剑来历与用途、瑞杏与伪神的关系、
   五行神是否会干预、觉醒触发细节、梁峰成长路径、**多周目↔多档互文**、结局轴。
5. **泥教细节**（用户搁置，待重设）。
6. 老待办：环境事件层（替代骰子）、信息边界两段式、知识库层、活跃→不活跃淘汰规则。

---

## 七、本轮改动文件（53 项，未提交）

**新增（未跟踪）**：`故事大纲.md`、`世界设定·读本.md`、`天气系统.md`、`character_expressions.py`

**后端**：`engine.py`、`llm.py`、`save_pipeline.py`、`删档.md`、`tools/{registry,state,money,weather_system,folder_to_prompt,battle_session,expression_sim}.py`、
`verify_expression.py`、`游戏数据/{基本信息,状态}.json`、`天气数据/{东部,南部}城市.json`
（删除：`tools/find_specific_expression.py`、`游戏数据/混乱度.json`）

**前端**：`trpg-client/src/in-game/GameController.tsx`（去掉"伤势"显示）

**世界观**：`主持人/{世界,总览,可能性骰}.md`、`江湖势力/{泥教,锦香宫,势力总览,唐门,极乐教,武力排名}.md`、
`角色静态档案/{温夫人,刘鄂,王二壮,赵逵,车轩辕,唐铮,瑞笙,梁有诗,孟仙谣,南溪,夏灵犀,画中仙,马富贵,万里鹏程}.md`、
`势力介绍.json`、`战斗系统.md`、`废弃/README.md`

**地图数据**：`trpg-map/数据/岳阳_布点锚点.json`

**文档**：`README.md`、`TODO.md`
