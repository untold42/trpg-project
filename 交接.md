# 交接 — 2026-09-20（技能树 + 相机 + /facility 收尾）

> 上一轮（2026-09-19：养成 v1 + 五行神庙 + 城门 + 详细页）见 README §16.18。
> 项目总况 → `README.md`；待办 → `TODO.md`；世界观 → `trpg-world/`。
> **本轮改动已提交 git**（连同上一轮尚未提交的养成/神庙/城门/详细页一起提交）。

---

## 〇、一句话

把养成从「熟练度自动解锁招式」重构成 **技能树**（技能点点亮，前后端全套，**零大模型**）；
顺手把 `/facility` 背景改成**后端确定性映射**（**删掉 `/facility/ui` 与小模型调用**）、
相机改成**玩家居中 + 禁止拖动**、存档加「**传闻 ≠ 事实**」。

---

## 一、技能树（本轮核心）

### 1.1 数据（server 端）
- **`trpg-server/技能树.json`（正典）**：22 节点（水 6 + 火/金/木/土 各 4）。字段：
  `五行 / 花费 / 前置[] / 要求{熟练度,剑法,…} / 内力 / 射程 / 范围 / 威力 / 连击 / 效果[] / 描述`。
  **⚠️ 内容是草稿，等用户改**（招名 / 数值 / 前置 / 要求）。
- **`trpg-server/招式表.json`（运行表）**：玩家**当前可用**招式，初始只有 `普通攻击`；点亮后由 `skill_tree` 写入。
- **`trpg-world/招式表.json` 已删**（搬来 server）。
- `属性.json`：加 `技能点`；`五行.<行>.招式[]` = 已学（名/描述/状态）。

### 1.2 逻辑（`tools/核心/skill_tree.py`）
- `view()` 全貌 + 状态；`learn(name)`：**前置 + 熟练度/基础数值 + 技能点**全达标 → 扣点 → **同步写** `属性.json` + `招式表.json`；`add_point` / `roll_point`。
- **技能点获取**：修行/练习（设施 A 类点数）**1%**、真实战斗结束 **2%**（模拟战不算）。概率在 `成长.json.技能点概率`（热改）。
- `growth._unlock_moves`（**熟练度自动解锁**）**已删**。
- **五行熟练度 → 战斗伤害系数**：`battle._compute_damage` 里 `威力 × (1 + 熟练度 × 0.01)`（每点 +1%）。

### 1.3 接口
- `GET /skilltree`（全貌 + 状态，不调模型）、`POST /skilltree/learn {name}`。

### 1.4 前端（`in-game/SkillTree.tsx` + `styles/SkillTree.css`）
- 菜单「技能树」→ **16:9 画布**（`STAGE = 1920×1080`）→ `Galaxy` 星空（**不监听鼠标**）→ **五神穹顶弧**（左下→右下，五等分）→ 点神淡入该行技能树（**白珠链** + 三态）→ 点节点出详情卡 → 点亮。
- 神名随神倾斜并向外移；进某行时星空染该神色（`st-tint`，浅）。**全程零大模型**。
- 参数集中在 `SkillTree.tsx` 顶部：`GALAXY`（星空）/ `APEX_RATIO`（弧高）/ `TILT_STEP`（倾角）/ `NAME_OUT`（名字外移）/ `GODS[].上移`。
- **神像**：`src/assets/神明/<行>.png`（`import.meta.glob` 读；**assets 不入 git**）。
  做图流程见 `五行/ai/README.md`（人画轮廓 / 机器「分区取边界 + 填洞取外轮廓」再平滑）。

---

## 二、`/facility` 收尾

- `GET /facility` 一次返回 **选项 + 背景**：选项查 `facilities.json`；**背景确定性**——查 `场景映射.md` 的 kind→场景 + 按地名**稳定散列**取一个。
- **删除** `GET /facility/ui` 与 `facility_ui`（不再调小模型）。设施不在表里也返回通用选项 + 背景。
- 前端：一次 fetch → 预加载背景图 → 开面板（不再有"先黑/先显旧背景"）。
- `场景映射.md` 补 5 座**五行神庙** + **相扑棚**。

---

## 三、相机（`Map.tsx`）

- 探索模式：玩家图标**始终居中**、**禁止拖动**（`dragging={!lockZoom}`）。
- 逐帧 `_rawPanBy` 把玩家拉回正中，带 `<1px` 死区防抖（避免静止时反复微调、每 100ms 触发 move）。
- 菜单总览图不受影响（仍可拖/缩放）。

---

## 四、存档规则

- `trpg-world/存档流程.md` 新增「**传闻 ≠ 事实**」：NPC 口述可能真、可能假；
  先记进**说话人**档案，未证实的用 `kind:"rumor"`，**不当客观事实**。

---

## 五、上手须知（务必读）

1. **后端必须重启**；前端 `npm run dev`（改 TS 只 `npx tsc -b`；**不要 `npm run build`**）。
2. **`trpg-client/src/assets` 不入 git**（背景 / 神像 / 音乐）——换机器要单独拷。
   `五行/ai/`（我生成的线稿/脚本）在仓库**之外**（用户桌面）。
3. 只跑一个 `python main.py`；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
4. `GameController.tsx` 是 CRLF：多行替换会失配，用单行匹配。
5. Vite dev server 会缓存空模块 → 白屏：**重启 dev server**（浏览器硬刷新没用）。
6. 地图命令城市化：单步脚本带 `TRPG_CITY=<城市>`。
7. 禁止从项目根全盘扫描（`find .` / `grep -r .`）：`node_modules/`、两万多张瓦片、存档会让命令卡几分钟。

---

## 六、下一步（详见 `TODO.md` ⚑）

1. **技能树内容/数值调参**（22 节点草稿待改；伤害系数 / 技能点概率 / 曲线）。
2. **五神像美术迭代**（5 张线稿已接入）。
3. 详细页四样式（`n1..n4`）+ 文字颜色（用户未拍板）。
4. 战斗时间折算接入（`clock.begin_battle/end_battle` 已就绪）。
5. 世界地图 / 跳城旅行；环境事件层；战斗路线 B（真战棋）。
6. 大工程未动：**信息边界**、**知识库层**。

---

## 七、本轮关键文件

**新增（server）**：`技能树.json`、`招式表.json`、`tools/核心/skill_tree.py`；上一轮：`facilities.json`、`成长.json`、`tools/大模型/facility.py`、`基础设施活动表.md`、`游戏数据/加成.json`
**新增（前端）**：`in-game/SkillTree.tsx`、`styles/SkillTree.css`、`in-game/Galaxy.tsx`、`styles/Galaxy.css`、`in-game/FacilityPanel.tsx`、`styles/FacilityPanel.css`
**改**：`main.py`、`battle.py`、`battle_session.py`、`growth.py`、`ui_sim.py`、`registry.py`、`time_flow.py`、`map_query.py`、`GameController.tsx`、`Map.tsx`、`ClickableLayer.tsx`、`walkable.ts`、`场景映射.md`、`战斗系统.md`、`存档流程.md`、`故事大纲.md`、`主持人/总览.md`、`README.md`、`TODO.md`
**删**：`trpg-world/招式表.json`
