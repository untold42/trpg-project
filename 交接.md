# 交接 — 2026-09-19（工具重组 + 一批机制修复 + 前端三件 + 岳阳地图修数据）

> **本文件只讲"这一轮改了什么、现在什么状态、下一步"。**
> 项目总况 / 架构 / 状态 → `README.md`；待办 → `TODO.md`；
> 世界观（导演用）→ `trpg-world/故事大纲.md`；GM 的世界设定 → `trpg-world/主持人/`。
>
> **⚠️ 本轮改动全部未提交 git**（约 35 改 / 48 删 / 新增一批）。tools 重组在 git 里表现为
> `D 旧路径` + `?? 新目录`，**`git add -A` 时 git 会自动识别成 rename**，历史不丢。

---

## 〇、一句话

这一轮做了七件事：**① 把 `tools/` 拆成 `大模型/小模型/导演/核心` 四个包**；
**② 时间/金钱/模式三条"台词与元操作"的机械化**；**③ 前端三件（总览图迷雾 / 按钮悬停 / 背景转场预加载）**；
**④ 大模型输出 JSON 容错 + 场外话（OOC）规范**；**⑤ 岳阳地图"陆地上的船"数据修复 + 生成器加固**；
**⑥ 对齐（属性全 0 / T6 / 战斗字段 / 导演接《故事大纲》）**；**⑦ 探索回地图改为确定性 + 探索换通用 BGM**。

---

## 一、工具重组（结构性，最需要看）

`trpg-server/tools/` 原本 49 个 `.py` **平铺**，现按归属拆成 4 个子包（各带 `__init__.py`）：

```
tools/
├─ 大模型/  26  registry money bag state ability location time_weather dice modes
│                get_character DB character_archive file_tools event_dice accident
│                folder_to_prompt factions recap place_recall difficulty_settings
│                battle battle_ai battle_runner battle_tactics battle_session battle_settings
├─ 小模型/   6  small_model ui_sim expression_sim world_sim world_worker character_expressions
├─ 导演/     1  director
└─ 核心/    16  state_manager ui_events map_query world_threads game_clock time_flow
                derived hunger weather_system macro_timeline mem_store audit explore
                movement place_hours map_settings
```

- **import 全部改写**：`from tools.X` → `from tools.<包>.X`（43 个 .py）。
- **21 处 `Path(__file__)` 路径锚点 +1 层**（这是最大的坑，已逐项验证：游戏数据 / 天气数据 /
  `时间影响.json` / `移动.json` / `trpg-project` / 前端 `assets/背景` / `故事大纲.md` 全部指向正确）。
- **依赖方向（无环）**：`大模型 → 核心`、`小模型 → 核心`、`大模型 → 小模型`（`battle_session`/`recap` 用
  `ui_sim`；`battle_ai` 用 `small_model`）、`核心 → 小模型`（`time_flow → world_worker`，跨日推演，绕不开）。
  **`小模型/` 从不反向 import `大模型/`** —— 这是"模型解耦"的体现：大模型本身不调用小模型，
  是小模型服务被引擎/战斗/回顾这些**代码子系统**调用。
- 相关工具：一键搬运用了脚本（建包→移动→批量改 import→修 `__file__`）。验证：`compileall` 通过 +
  冒烟（`/maps` `/state` `/factions` `/settings` `/battle/roster` 全 200，`TOOLS_MAP=29`、`ALL_TOOL_NAMES=24`）。

> 新增可复用脚本：`trpg-map/修复水域POI.py`、`trpg-server/director_probe.py`（见下）。

---

## 二、时间 / 金钱 / 台词护栏（规则 4 / 9 / 12 的机械化）

### 2.1 时间：新增 `advance_time`
- 新增 **`advance_time(ke=… / shichen=…)`**（推进 N 刻 / N 时辰，至少 1 刻）；
  **`update_time` 收窄为"直接设置日期/时辰/刻"**（删掉 `advance_ke/advance_shichen`）。
- 台词/场外/观察回合：`advance_time` ≤ **2 刻**、`update_time` 与 `sleep` 一律驳回。

### 2.2 金钱：账本 + 只扣一次
- `modify_money` 增可选 `reason`；`金钱.json` 增 **`最近支出`（最近 3 笔扣费，新在前）**。
- 规则 4：「**同一笔花销只扣一次**；扣费前先看 `金钱.json.最近支出`」。

### 2.3 台词/场外/观察回合「禁止改任何数值」——机械护栏
`engine._say_time_guard` 现在同时拦：`modify_money / modify_item / add_item / remove_item /
modify_hunger / modify_hp / modify_tp / modify_health / update_location / update_weather`（全部驳回并回错给模型）。
放行：`start_battle`、只读工具。
- **副作用（设计本意）**：`say`（说话）里点菜/付钱**不再扣钱**——要花钱用「行动」。

---

## 三、模式切换（叙事 ⇄ 探索）

- **「探索」按钮改为确定性元操作**：`main.py` 识别固定请求「进入探索」→ 传 `force_explore=True` →
  `engine` 直接补发 `ui_event("mode", mode="explore")`，**不再依赖 GM 调工具**（此前 GM 会漏调，
  按三次才切）。`总览.md` 规则 10 已同步：「系统自己切，GM 只把离场场景收尾」。
- **`resume_exploration` 工具退役**：进 `registry._DISABLED`（代码保留，`TRPG_RESUME_TOOL=1` 回滚）。
- **探索地图换通用 BGM**：叙事→探索时，`ui_sim.explore_track()` 叫小模型从**无绑定的通用曲**里挑一首
  （默认 `山中好岁月`）——不再把青楼/酒楼的场所专属曲带到大地图上。

---

## 四、前端三件

1. **总览图（菜单地图）加入迷雾**：删掉 `showAllIcons`，改为**按已探索足迹**过滤 POI（未去过的
   不显示、不可点）。`README §13.4` 血泪坑 #7 已改。调试 `?nofog=1` 可关。
2. **按钮悬停**：`未选中时按钮.png` = 常态，`金红矩形按钮.png` = `:hover`；覆盖 11 个按钮 +
   `.sm-toggle`（`GameController.css` / `StaggeredMenu.css`）。
3. **背景转场预加载**：`BackgroundTransition.tsx` 改为**先 `new Image()` + `decode()` 完成再交叉淡化**，
   修「淡出后淡入还是旧图、随后啪一下换新图」；`FADE_MS` 与 CSS 动画对齐 500ms。
- 另：`ClickableLayer.tsx` 的 **`GEO_VERSION` v1→v2**（强制前端丢弃 IndexedDB 里的旧 `clickable.geojson`）。

---

## 五、大模型输出容错 + 场外话（OOC）

- **JSON 容错解析**：`engine.parse_instruction_array()` —— 容忍 ``` 围栏、前后废话、
  **`content` 里未转义的英文双引号**（实测抓到过：对白写 `"…"` 把 JSON 截断，连重试都失败 → 占位旁白）。
  `_parse` 与 `parse_instructions`（历史面板 / 存档誊写）共用它。
- **history 存规范 JSON**：`normalized_assistant()`（日志仍留原始 `assistant_raw`）。
- **OOC 规范**：`总览.md` 新增「场外话」一节 + 每轮系统提醒 `engine.OOC_NOTE` ——
  前缀「玩家的对主持人说的话：」是**元对话**，要**现代白话直答**，禁止文言/话本/入戏（GM 曾在场外
  用「某」「你正立在…」回答）。探索按钮的固定请求是唯一例外（那条要叙述离场）。

---

## 六、岳阳地图数据修复（"城里有船行/画舫却没水"）

- **病根**：① `build_world.py` 的 `edge` 池（随机撒点、不看水）混了 `渡口/码头`；② 岳阳冻结表
  `岳阳_POI.json` 是从"半成品世界"冻的，水岸对不上。
- **修复脚本** `trpg-map/修复水域POI.py`（可复用）：水类 kind（画舫/船行/码头/渡口/鱼行/浮桥/桥）
  离水 ≤60m 不动、60m–2km **吸附到最近水岸**（优先大水面、沿岸线铺开）、>2km 删除。
  岳阳：岸边不动 38 / 吸附 119 / 删除 2；修后**距水中位 0m、最大 57m**（修前最远 2675m）。
- **生成器加固**：`edge` 池去掉水类 kind、`general` 池去掉 `桥`；新增
  `WorldBuilder._repair_water_pois()`（冻结表加载时校验，幂等）。
- **重导出**：`生成.py --city 岳阳 --only clickable` + `--only db`（**未重生成瓦片**，POI 不在瓦片里）。
  前端需配合 `GEO_VERSION` bump。

---

## 七、对齐 / 修复

- **属性全 0、T6 起步**：`属性.json` 五行全 0、`基本信息.武器/梯度` 对齐（T6·三流）。
- **战斗字段**：`battle._player_skills` 兼容 `五行`（新）/`五行剑`（旧）；`_tier_coef` 改为取前导 T 码
  （`T6·三流` → 0.25，此前会误落默认 1.0）。
- **导演接《故事大纲》**：`director.py` 把 `trpg-world/故事大纲.md` 作为 system 材料 + **首局无前情用
  当前状态兜底**；新增**剧透机械兼底**（黑名单命中 → 重试 → 仍漏则机械消弧）。诊断脚本
  `trpg-server/director_probe.py`（`--e2e` / `--no-outline`）。实测：首局 1 次调用 68s，487 字，干净。

---

## 八、环境变量开关（速查）

| 变量 | 作用 |
|---|---|
| `TRPG_RESUME_TOOL=1` | 回滚：重新下发 `resume_exploration` 工具 |
| `TRPG_FILE_TOOLS=1` | 启用文件工具（默认禁用） |
| `TRPG_EVENT_DICE=1` | 回滚：启用内容生成型骰子（默认停用） |
| `TRPG_DIRECTOR=0` | 关导演（GLM） |
| `TRPG_CLOCK=0` / `TRPG_WORLD_SIM=0` / `TRPG_UI_SIM=0` / `TRPG_EXPRESSION_SIM=0` | 关时钟 / 世界推演 / 背景音乐 / 表情 |

---

## 九、上手须知（务必读）

1. **后端必须重启** `python main.py`——**tools 目录结构变了**，运行中的旧进程仍是扁平 import。
2. **前端硬刷新**（Ctrl+Shift+R）——`GEO_VERSION` bump 要清 IndexedDB 里的旧 `clickable.geojson`。
3. **只跑一个后端**；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
4. **不要跑 `npm run build`**；前端用 `npm run dev`，改完 TS/TSX 只跑 `npx tsc -b`。
5. **前端白屏排查**：先怀疑 dev server 的**空模块缓存**（本会话踩过：`GameController.tsx` 被缓存成
   175 字节空模块 → 白屏）。重启 dev server 即好；`tsc` 通过不代表缓存没坏。
6. 地图命令**全部城市化**：单步脚本带 `TRPG_CITY=<城市>`。
7. **GM 常驻 = `主持人/`（全量）+ `游戏数据/*.json`（扫描即注入）**；想喂 GM 放 `主持人/`，
   想藏起来放 `trpg-world/` 根目录；**别把剧透丢进 `游戏数据/`**。

---

## 十、git 状态 / 提交建议

未提交。建议按主题拆提交：

1. **工具重组**：`tools/{大模型,小模型,导演,核心}/` + 全仓 import 改写 + `__init__.py`（git 会识别 rename）
2. **时间/金钱/模式护栏**：`engine.py` `main.py` `save_pipeline.py` `tools/核心/money.py` `tools/大模型/registry.py` `总览.md`
3. **前端三件**：`BackgroundTransition.tsx` `ClickableLayer.tsx` `GameController.tsx` `Map.tsx` 两个 css
4. **LLM 容错 + OOC**：`engine.py` `总览.md`
5. **岳阳地图数据**：`修复水域POI.py` `build_world.py` `岳阳_POI.json` `岳阳_南宋世界.json` `clickable.geojson`
6. **运动系统**：`tools/核心/movement.py` `移动.json` `GameController.tsx` `Map.tsx`
7. **文档**：`README.md` `TODO.md` `交接.md`

---

## 十一、下一步（按优先级）

1. **养成系统**（用户上轮提出，本轮因对齐没做）：`属性/五行熟练度` 成长、`train/worship` 工具、
   `修行.json`、招式按熟练度解锁——方案见对话记录，**未落代码**。
2. **战斗时间折算接入**（`clock.begin_battle/end_battle` 已就绪）。
3. **世界地图 / 跳城旅行**（跨城现在靠叙事/渡船）。
4. **环境事件层**（替代已停用的随机骰子；`TODO.md §十四`）。
5. **信息边界**（§九）/ **知识库层**（§十）——大工程，未动。

---

## 十二、本轮新增/改动的关键文件

**新增**：`tools/{大模型,小模型,导演,核心}/`（含 `__init__.py`）、`tools/核心/movement.py` + `移动.json`、
`trpg-map/修复水域POI.py`、`trpg-server/director_probe.py`

**后端**：`engine.py` `main.py` `llm.py` `save_pipeline.py`、`tools/大模型/registry.py`、
`tools/大模型/money.py`、`tools/大模型/battle.py`、`tools/大模型/time_weather.py`、
`tools/小模型/ui_sim.py`、`tools/核心/time_flow.py`、`tools/导演/director.py`（路径全随重组变动）

**前端**：`BackgroundTransition.tsx` `ClickableLayer.tsx` `GameController.tsx` `Map.tsx`
`styles/GameController.css` `styles/StaggeredMenu.css`

**数据**：`trpg-map/数据/岳阳_POI.json`、`岳阳_南宋世界.json`、`draw_tiles/build_world.py`、
`trpg-client/public/data/yueyang/clickable.geojson`（+ `mapicons/manifest.txt`）

**规则/文档**：`主持人/总览.md`、`主持人/可能性骰.md`、`战斗系统.md`、`故事大纲.md`、`世界设定·读本.md`、
`README.md`、`TODO.md`、`交接.md`

> 注：`trpg-world/主持人/世界.md`、`音乐清单.md`、`trpg-world/剧情介绍（通俗版）.md` 的改动**不是本轮**（用户自行编辑）。
