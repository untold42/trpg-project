# 交接 — 2026-09-25（背景系统重构 + 美术全量 + 美化 + 仓库瘦身）

> 项目总况 → `README.md`；本轮变更见 `README/11-实现记录.md §16.22`；待办 → `TODO.md`；世界观 → `trpg-world/`。
> 上一轮（2026-09-23）：**任务系统（种子 / quest_arbiter / quest_sim）+ tools 分包（大模型/小模型/战斗/服务/核心/开发）+ 保活 + 好感度**，
> 已随 checkpoint 提交（`b235c87`），设计见 `trpg-server/文档/设计-任务系统与保活机制.md`。
> 再上一轮（2026-09-21 晚）：机制硬化 + 养成重构 + 天气/驳回 + 缓存优化（`README §16.21`）。

---

## 〇、一句话

把**背景系统**从「一张 kind 一张图 + 动词门禁猜切换」重构成
**「场景表 JSON + 城内/城外/室内三分类 + 城外静默闸门 + 候选集合 + 小模型按最近几轮语义选」**；
美术侧**342 张全部出齐**并做了**统一美化（去噪 + 曝光 -12% + 归一化 1920×1080）**；
顺手治了**启动预加载 2.2G**的隐性灾难，并把仓库从 **7G 瘦到 2.7G**。

---

## 一、背景系统（本轮核心）

### 1. 三个文件夹 = 三分类（`背景_重构/`）

```
背景_重构/
├── index.ts / 主页面.png
├── 城内/  (15 场景)   坊 坊巷 广场 市集 城门 …
├── 城外/  (39 场景)   荒野 田野 山路 溪畔 山 林 湖 村 镇 码头 桥 亭 …
└── 室内/  (117 场景)  青楼 客栈 酒楼 寺 观 州衙 … + 全部内室（客房/闺房/灶房/阁楼…）
```

**唯一的闸门 = `背景_重构/城外/`：**

- 玩家**在城内**（`基本信息.位置.在城内 = true`）→ **所有背景都可用，不设限**；
- 玩家**在城外** → **只准用 `背景_重构/城外/` 里的**。

→ 以后要调「城外能用什么」，**只往 `背景_重构/城外/` 里放/拿文件夹**即可，不改代码。
（`城内/`、`室内/` 只是给人看的分类，代码不拿它们做限制。）

### 2. 场景表改成 JSON（单一真相源）

- **删** `trpg-world/场景表.md`、`trpg-world/背景场景重构清单.md`
- **新增** `trpg-world/场景表.json`：`场景说明` / `地点候选` / `兜底组` / `场景原型` / `分批`
- 地点候选从「**优先级链**」改成「**候选集合**」——含内室子场景，例如
  `青楼 → 青楼/厅堂/雅间/闺房/阁楼/浴房/灶房/后院/屋顶`；
  这样「青楼姑娘引我上阁」能切到 **阁楼**，而不会停在青楼。
- `场景说明` 与 art `note` 里**已清掉所有「城内/城外」字样**——闸门只做**静默过滤候选**，
  prompt 里一个字都不给地理提示。

### 3. 选背景链路（`tools/小模型/ui_sim.py`）

```
大模型回复 + 当前地点 + 最近 3 轮旁白
  → 代码：地点 → kind → 候选集合 ∩ 此刻可选（城内不限 / 城外只用城外集合）
  → 小模型：在候选里选一个 / 填「无」（不知道自己在城内还是城外）
  → 引擎：只做去重（同场景不重发）+ force 兵底
```

- **删掉** `_SCENE_SWITCH_RE` 动词门禁（`移步/引/领/送/上阁…` 覆盖不了；`他想起昨日出门` 又误判）。
- `generate(..., recent=)` 新增**最近 3 轮旁白**，让模型按整段语义判断。
- `scene_categories()` / `scene_keys()` 扫三层目录；`scene_for()` 返回该 kind 的**主场景**（探索「详细」页用）。

### 4. 自检

`trpg-server/开发/verify_scene_table.py`（只读，exit 0/1）查：
① 候选/兜底里的场景都有说明；② 被引用场景都有图或已登记；③ 有图目录都被引用；
④ 两城地图 kind 都已映射；⑤ song_kinds 都有场景原型。
当前：`有图 171 · 缺图 0 · ①②③④⑤ 全过`。

---

## 二、美术（全量完成 + 统一美化）

- **342 张全部出齐**（171 场景 × 白天/黑夜），已清掉缺时段/歪名（`田野/黑夜` 曾被存成 `Codex 图像 ….png`）。
- **统一美化**（脚本 `去噪点算法/beautify_backgrounds.py`）：
  `background` 预设（**不抬阴影**）+ 固定力度 + **曝光 -12%** + 归一化 **1920×1080**。
  - 原始 AI 图偏亮的主因是原脚本的 `shadow_lift`；新预设关掉它，再整体压 12%。
  - `denoise_beautify.py` 新增 `--exposure` 与 `background` 预设（这是用户桌面的独立小项目）。
- **原图已备份**并搬出仓库：`D:\trpg仓库\背景_重构_原图备份`（789M）。
- ⚠️ `主页面.png`（主菜单背景）**没被批处理**，仍是原图；要一致的话按同参数单独过一遍。

---

## 三、前端

1. **启动预加载瘦身（重要）**：`GameController` 的预加载 glob 原为 `../assets/**/*.{png,…}`，
   会把 `背景_重构`（988M / 342 张 1080p）+ 旧 `背景`（217M）+ **备份**（789M）**一次性 decode ≈2.2G**。
   → 加 `"!../assets/背景_重构/**"`（背景按需，`BackgroundTransition` 会先 preload+decode 再淡入）。
   预加载从 ~2.2G 降到 **~240M**（只剩 人物/ui/动画/神明）。
2. **历史记录框与文字同步**：打开历史前 `await new Image().decode()` 完 `历史记录框.png` 再 `setShowHistory(true)`，
   不再「文字先出、背景板后到」。
3. `background.ts` 加 `SCENE_CATEGORY` / `sceneDir()`——后端只发场景名，前端按分类目录还原路径
   （`背景_重构/<城内|城外|室内>/<场景>/<时段>.png`）；`ScenePicker` 同步适配三层 glob。

---

## 四、资产搬迁 + 仓库瘦身（7G → 2.7G）

| 动作 | 省 |
|---|---:|
| 旧参考图 `assets/背景`（217M）→ `D:\trpg仓库\背景_参考图`；`config.reference_root` 已改指 D | — |
| 原图备份 `assets/背景_重构_原图备份`（789M）→ `D:\trpg仓库\背景_重构_原图备份` | — |
| 删 `trpg-client/dist`（旧 build 产物，89208 文件） | 1.3G |
| 删 `background-pipeline/packs`（生成物，可重跑） | 2.1G |
| 删 `background-pipeline/重画`（重画素材，图已画完） | 80K |
| 删 `trpg-map/draw_tiles/tiles`（瓦片暂存；`public/tiles` 是成品） | 277M |
| `git gc --prune=now`（原 398M 松散对象 → 136M 单包） | 273M |

**保留**：`public/tiles`、`src/assets`、`draw_tiles/db`（运行时必需）、`node_modules`。
`trpg-map/数据` 仍有 ~423M（OSM 全量/精简/世界 json，且已提交进 git；要再瘦得搬 D + 重写历史）。

---

## 五、上手须知（务必读）

1. **后端必须重启**（`ui_sim.py` / `engine.py` 等改了）；前端 `npm run dev`，改 TS 只 `npx tsc -b`；**不要 `npm run build`**。
2. **前端硬刷新**（Ctrl+Shift+R）：背景三分类、取图路径、预加载排除、历史框同步都要重新加载。
3. **背景图不在 git**（`src/assets` 全 gitignore）；换机器要单独拷 `背景_重构`（现 988M，1920×1080）。
4. 只跑一个 `python main.py`；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
5. 禁止从项目根全盘扫描（`find .` / `grep -r .` / `du -sh .`）——本轮我犯过一次，被用户提醒。
6. 改背景后跑一次 `python trpg-server/开发/verify_scene_table.py`（几秒，0/1）。
7. 桌面的 `去噪点算法/` 是**独立小项目**（去噪+美化+曝光），与游戏无依赖；批量脚本 `beautify_backgrounds.py` 在那。

---

## 六、待办 / 待拍板

1. **`主页面.png` 未美化**（可选，同参数单独过）。
2. **`trpg-map/数据` OSM 大件**（岳阳全量 83M + 扬州全量 73M + 岳阳世界 76M）若不再重建地图 → 搬 D + `git rm --cached`；
   要彻底瘦 `.git`（仍 137M）需 `git filter-repo` 重写历史。
3. `音乐 275M` / `人物 233M` 可考虑转 webp（省一半以上）。
4. 地点候选目前是**手写集合**；后续加 kind/加图，记得同步 `场景表.json`（跑自检兜底）。
5. 其余见 `TODO.md ⚑`：小游戏接入、技能树调参、五神设定、详细页四样式、信息边界/知识库层等。
6. 任务系统（上一轮）**前端未做**：任务按钮/面板/HUD 追踪条 + `/action` body 带 `追踪: [id…]`。

---

## 七、本轮关键文件

**新增**
- `trpg-world/场景表.json`（单一真相源）
- `trpg-server/开发/verify_scene_table.py`
- （桌面独立项目）`去噪点算法/beautify_backgrounds.py`

**删除**
- `trpg-world/场景表.md`、`trpg-world/背景场景重构清单.md`
- `trpg-client/dist`、`background-pipeline/{packs,重画}`、`trpg-map/draw_tiles/tiles`

**改（后端）**：`tools/小模型/ui_sim.py`（三分类 / 静默过滤 / recent / 删动词门禁）、
`engine.py`（`_recent_narrations` / 删 `allow_bg`）、`tools/大模型/facility.py`、`main.py`

**改（前端）**：`in-game/background.ts`（`SCENE_CATEGORY`/`sceneDir`）、`in-game/GameController.tsx`（预加载排除 + 历史框同步）、
`in-game/ScenePicker.tsx`

**改（流水线）**：`background-pipeline/{config.json,build_packs.py,README.md}`（读 JSON / `extra_kinds` / 窄景 / priority / 参考图移 D）

**改（文档）**：`README.md`、`README/09`、`README/11`、`README/13`、`trpg-world/音乐表.md`、`.gitignore`（`重画/`）

**提交**：`c99b199 refactor(背景): 场景表 JSON 化 + 三分类 + 城外静默闸门`
（上一轮 checkpoint：`b235c87 任务系统 + tools 分包 + 背景重构 + 场景候选映射`）
