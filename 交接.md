# 交接 — 2026-09-21 晚（机制硬化 + 养成重构 + 天气/驳回 + 缓存优化）

> 项目总况 → `README.md`（本轮变更见 §16.21）；待办 → `TODO.md`；世界观 → `trpg-world/`。
> 上一轮（2026-09-21 白天）：五行 = 灵知 + 场景/音乐表合并 + 技能树扩展；当前以 `技能树.json` 实际统计的 44 节点为准（README §16.20）。

---

## 〇、一句话

把一批「靠提示词自觉」的地方改成**代码裁决**（设施耗时 / 意外 / 天气 / 位置 / 进食 / 地点同名）；
养成改成**玩家自选时长 + 收益递减 + 一天一个属性一次**；新增**「驳回上轮」**；把上下文缓存命中上限从 ~73% 抬到 ~88%；
探索模式加了**天气动效**。

---

## 一、世界观（以 `trpg-world/主持人/世界.md` 为准）

- 五行 = **灵知**（独特思考 + 坚韧意志）；神**一次性授予**，授予后**可练到极致**。
  旧句「神是流量」「五行练不出来」已删。`成长.json` 的「五行熟练度」= 把授予的力量练到极致，自洽。
- 两轴：源泉（灵知 / 神，一次性）× 技艺（自己练）。
- 三大世家 = 唐门 + 南宫 + 上官（唐门属中间层，南宫·上官近伪神）——已清各文档旧表述。
- 瑞杏 = 五行俱全（弑神者）；`世界.md §七`「伪神亲赐通灵术与长生」= 外间所知的一面。
- `宏观时间线.json` 的 `辐射范围` 去掉「扬州」，改区域化（42 处）。

## 二、机制硬化（代码裁决）

- **设施耗时**：`use_facility` 结算后**后端自动按刻数推时间**；引擎在设施已推时间后**驳回** `advance_time`；时间权威检查把设施耗时算作已落库。
- **意外**：本回合触发意外 → 设施活动**不结算、不推时间**，工具返回里告知 LLM 按失败叙述。
- **进食**：`modify_hunger` 传正数时，要求玩家本轮动作含「吃/喝/…」；否则驳回（治「点单加一次、真吃又加一次」）。
- **天气**：输出里与 `基本信息.天气.状况` 不符的天气词 → 带修正**重发一次** → 仍不符则**机械改写**（雨→云、雪→霜、雷→风、雹→霰）。软提示已删。
- **位置 / 出城**：`_gate_note` 注入「当前地点 + 落脚点真实周边」；`query_place` 同名按**距玩家**排序；`location.py` 回填「地点」优先**具体地点**（治玄冥庙瞬移、君山岛标签）。
- **新模块** `tools/大模型/turn_context.py`：每轮记「玩家动作 + 模式」，供工具判定（进食门禁用它）。
- **战斗数值配置化**：新增 `trpg-server/配置/战斗数值.json`（梯度、命中、Buff、武器、NPC 推导、战术 AI、思路评分的唯一策划源）及 `tools/核心/battle_config.py` 校验；`battle*.py` 只保留公式与结算。支持热读，无需重启后端。
- **天气修正结构化**：新增 `trpg-server/配置/天气.json` 与 `tools/核心/weather_config.py`；`基本信息.天气.影响` 改为判定修正/倍率/禁用/行动门禁/环境标签对象，`影响文本` 由代码派生。配置支持热读，同日也会自动同步。

## 三、养成重构

- `成长.json` 新增 `时长曲线`：`2→×1、4→×1.5、8→×2、16→×4`（点数 = 基准 × 系数；1 时辰 = 8 刻）。
- `use_facility(..., ke=)`：`ke` = 玩家自选修行刻数（工具 schema + 规则 15 已同步；1 时辰 = 8 刻，2 刻起步）。
- **一天一个属性只能练一次**：`游戏数据/养成记录.json`；跨设施同属性也算（棋馆+书院的知识）。
- 五神庙「静修」基准 `2 → 0.5`、`耗时刻 1 → 2`。
- 技能点概率已从初版 1%/2% 上调，并统一改由 `成长.json.技能点概率` 热配置；当前值以配置文件为准。
- **棋馆取消养成**（练习/挑战 → 自由）：下棋收益留给后续小游戏。
- 前端「详细」页：点数类设施底部出现**时长选择器**（2/4/8/16 刻，默认 8）；`facility_detail` 每选项加 `效果类型`。
- ⚠️ 未拍板：时长曲线目前「长时段单位时间效率更低」（2刻 0.25/刻 vs 8刻 0.125/刻）→ 理性玩家总选 2 刻。要不要反过来（长练更划算）？

## 四、「驳回上轮」

- 后端：`TurnRunner.run()` 回合前**全量快照**（`游戏数据/*.json` + 时钟 + `current.jsonl` 长度 + `history` 长度 + 会话内存 + 意外标志）；`reject()` 回滚后用**同一输入**重发；`POST /reject`。
- 前端：菜单（☰）里「放弃本轮」下面多了 **「驳回上轮」** → 替换上一轮叙事（截 history 再拼）。
- 可反复驳回；意外不重抽；`world_sim` 异步队列不回滚（影响极小）。

## 五、上下文缓存 + 推理模型

- 面板实测命中率 ~73%：原因是**两份全量状态快照**（上一轮 + 当前）落在「每轮必变」的尾巴里。
- 改：① 上一轮状态 → 代码算的**「本轮变化」差分**；② `导演简报/地图设置/难度设置` 从状态尾巴挪到**前缀**（`===== 长期设定 =====`）。上限 **73% → ~88%**。
- `folder_to_prompt` 加 `sorted()`（规则顺序是缓存前缀最前部，不能飘）。
- **`deepseek-v4-flash` 是推理模型**（一句闲聊实测 7.8k reasoning token，代码把 `reasoning_content` 丢掉了）。
  `reasoning_effort` 实测：`low`/`disabled` 会让 GM **不调工具**（机制全废）；`high`/默认/`max` 才正常 → **GM 保留思考**。导演（每局一次）可考虑 `max`。

## 六、天气动效（前端，仅探索模式）

- `in-game/WeatherLayer.tsx` + `styles/WeatherLayer.css`：雨/雪（Canvas 粒子）、阴（灰罩 + 云影）、晴（暖阳）。
- 挂在探索层（`gameMode === "explore"`），`pointer-events:none`、`z-index 550`（低于 POI 标记）；夜晚不出太阳。
- 数据源：`基本信息.天气.状况`（每次 `/state` 刷新，不主动轮询）。

## 七、音乐 / BGM

- 地点专属曲：当前地点/类型**唯一绑定**时**硬选**（锦香宫→锦绣织岁…）；引擎允许「背景没变也切主题曲」。
- **曲牌去重**：抽输出里的 `《…》` 记进 `游戏数据/曲牌.json`，下一轮注入「近期用过…勿重复」（治 GM 每次青楼都写《鹧鸪天》）。
- **主菜单主题曲《子夜寄君书》移到 `assets/音乐/固定/`**，从 `音乐表.md` 叙事节删除 → AI 不再选到它（原来它同时是 AI 候选，会被当夜曲选中）。

## 八、地图可走

- `trpg-map/draw_tiles/export_walkable.py` 新增 `dock` 图层（渡口/码头）；前端 `walkable.ts` 加 `DOCK_R=80`（水域里码头 80m 内可走）。
- 治「卡在君山渡」（它在「君山岛环水」里、最近陆路 9.2km）。已重导两城 `walkable.geojson`。

---

## 九、上手须知（务必读）

1. **后端必须重启**（本轮大量 `.py` 改动）；前端 `npm run dev`（改 TS 只 `npx tsc -b`；**不要** `npm run build`）。
   热生效不用重启：`主持人/*.md`、`facilities.json`、`成长.json`、`音乐表.md`。
2. 前端**硬刷新**一次：天气动效、「驳回上轮」按钮、时长选择器、渡口可走（geojson）都要重新加载。
3. `trpg-client/src/assets` 不入 git（背景 / 神像 / 音乐）——换机器要单独拷。
4. 只跑一个 `python main.py`；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
5. `GameController.tsx` 是 **CRLF**：多行替换会失配，用单行匹配（`\r\n`）。
6. Vite dev server 会缓存空模块 → 白屏：重启 dev server（浏览器硬刷新没用）。
7. 地图命令城市化：单步脚本带 `TRPG_CITY=<城市>`。
8. 禁止从项目根全盘扫描（`find .` / `grep -r .`）：`node_modules/`、两万多张瓦片、存档会让命令卡几分钟。
9. 中途验证脚本会改 `游戏数据` —— 先备份、跑完还原（本轮测试都这么做的）。

---

## 十、待办 / 待拍板（详见 `TODO.md` ⚑）

1. **小游戏接入**：棋馆已腾空（下棋收益留给小游戏）。
2. 技能树数值/内容调参（44 节点、总花费 112）；技能点概率（以 `成长.json` 为准）、成长曲线。
3. 养成平衡：时长曲线方向（短练更优 vs 长练更优）；**内力/暗器/轻功仍无设施来源**（内力完全没有）。
4. 五神具体设定 + 神像美术。
5. 详细页四样式（`n1..n4`）+ 文字颜色。
6. 战斗时间折算接入；世界地图/跳城；环境事件层；战斗真战棋。
7. 大工程：**信息边界（§5.7 两段式）**、知识库层。信息边界本轮只靠 GM 守则 + 硬事实，没做机械隔离。

---

## 十一、本轮关键文件

**新增**：
- 前端：`trpg-client/src/in-game/WeatherLayer.tsx`、`src/styles/WeatherLayer.css`
- 后端：`trpg-server/tools/大模型/turn_context.py`
- 运行数据：`trpg-server/游戏数据/养成记录.json`、`曲牌.json`

**改（后端）**：`engine.py`（GM 守则 / reject / 缓存 A+B / 天气门禁 / 曲牌）、`main.py`、`facilities.json`、`成长.json`、
`tools/大模型/{facility,state,location,registry,battle_session,accident,time_weather,folder_to_prompt}.py`、
`tools/核心/{growth,map_query}.py`、`tools/小模型/ui_sim.py`、`基础设施活动表.md`

**改（前端）**：`GameController.tsx`、`FacilityPanel.tsx`、`walkable.ts`、`Menu.tsx`、`music.ts`、`SkillTree.tsx`、
`styles/FacilityPanel.css`、`styles/SkillTree.css`、`ClickableLayer.tsx`、`Staggered Menu.tsx`

**改（地图）**：`trpg-map/draw_tiles/export_walkable.py` + 两城 `walkable.geojson`

**改（正典/文档）**：`主持人/{世界,总览}.md`、`故事大纲.md`、`音乐表.md`、`江湖势力/*`、`角色静态档案/*`、`世界推演/宏观时间线.json`、`README.md`、`TODO.md`
