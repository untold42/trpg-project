# 交接文档 —— 前端地图性能（探索模式卡顿）

> 写给下一个接手的人（AI 或人）。**请先完整读完再动手。**
> 本文件只讲「探索模式地图卡顿」这一件事，其他背景另见 `STATUS.md` / `ARCHITECTURE.md`。
>
> 最后更新：**2026-09-16 已定案并修复**（性能主因 = HitLayer 每次重建；图标缓存 bug 已修；逐层开关已实现）。
> 注：上一轮的地图性能改动**已提交**（`57da480`「地图」），不是未提交。

---

## 0. 一句话现状

**主因已定案并修复（2026-09-16）。**

用户逐层关实验的结果：

| 测试 | 结果 | 结论 |
|---|---|---|
| `?nohit=1`（关命中层） | **不卡** | → **HitLayer 是主因** |
| `?noicons=1`（关图标层） | 卡 | → 图标层不是主因 |
| `?nopan=1`（关相机） | 稍有掉帧 | → 相机是次要因素 |
| `?nofog=1`（全渲染） | **最流畅** | → 迷雾让 `data` 频繁变 → HitLayer 重建 |

**根因**：`HitLayer` 的 effect 依赖 `[map, data, extents]`。迷雾开着时每走一段
`visibleFeatures` 就是新数组 → `data` 变 → **整个 canvas 图层被拆掉重建**
（新建 `L.canvas()` + `clearLayers()` + `addData()` 几百个 `L.Path`）。nofog 时
`data` 恒等于 `features`，effect 不重跑，所以流畅。这正是文档 §6 的「假设 C」。

**已修**：见下方 §6/§7.3 与 §11 定案记录。**还有残余**：`?nopan=1` 仍稍有掉帧，
疑似瓦片首次加载/首次放大合成（假设 A/D），未处理。

---

## 1. 症状（用户原话，非常重要）

| 时间 | 用户描述 |
|---|---|
| 最早 | 「移动到相机边界时会卡顿一下切换」 |
| 中途 | 「我跑到了城南，现在想往南走，很卡，几乎动不了」 |
| 中途 | 「现在我走到城东卡了」 |
| 中途 | 「即便全部渲染，菜单地图还是很流畅。但是探索模式的地图就是很卡」 |
| 中途 | 「菜单地图的 z18 也非常流畅」 |
| 中途 | 「low 帧 1 帧，最高 165 帧。还是卡」 |
| **最后（最关键）** | **「我之前走过的地方很流畅，新走的地方依旧卡」** |

### 已确认的对照事实

| 场景 | 表现 |
|---|---|
| 菜单地图（`showMap`，**无 WASD**，全部 2344 个 icon，z16 与 z18 都试过，**平移**） | ✅ **流畅** |
| 探索地图（WASD，z18，视口内一部分 icon） | ❌ **卡** |
| 探索地图 **重走已走过的区域** | ✅ **流畅** |
| 探索地图 **走进没走过的区域** | ❌ **卡** |

> ⚠️ 最后两条是**最强的线索**，请优先从这里查。

---

## 2. 如何跑起来

```bash
# 后端（Flask :5000）—— 必须，否则 /explored 拿不到玩家位置
cd trpg-server && python main.py

# 前端（Vite，默认 :5173）
cd trpg-client && npm run dev
```

- 地图数据（瓦片/图标/geojson）已经生成好并放在 `trpg-client/public/`，**不需要重新生成**。
- 改 `*.py` 要重启后端；改前端 Vite 会热更，但 **改了 hook 顺序后必须硬刷新（Ctrl+Shift+R）**，否则 React Fast Refresh 会失效甚至白屏。

---

## 3. 系统架构（与性能相关的部分）

### 3.1 数据规模（实测）

| 项 | 数值 | 位置 |
|---|---|---|
| 瓦片 | **30561 张 / 133 MB**（z11:28, z12:104, z13:375, z14:1500, z15:5796, **z16:22758**） | `trpg-client/public/tiles/` |
| 可点要素 | **2344 个 / 0.73 MB** | `trpg-client/public/data/clickable.geojson` |
| 图标 | 118 张 PNG（46 个键 × `day.png`+`night.png`，另有 13 个键备而未用） | `trpg-client/public/mapicons/<键>/` |

> ⚠️ **没有 z17 / z18 瓦片**。`TileLayer` 用 `maxNativeZoom={16}`，
> 所以 **z18 下显示的是 z16 瓦片被放大 4 倍**。

### 3.2 前端链路

```
GameController.tsx            三模式状态机（explore / narrative / battle）
  ├─ GameMap (Map.tsx)        探索：<GameMap zoom={18} lockZoom wasd focus={cursor} .../>
  │    ├─ <MapContainer>      react-leaflet v5 + Leaflet 1.9
  │    │    ├─ <TileLayer url="/tiles/{z}/{x}/{y}.png" minZoom=12 maxZoom=18 maxNativeZoom=16 noWrap/>
  │    │    ├─ <ClickToMove>  点地图回调
  │    │    ├─ <ClickableLayer footprints={...} focus={cursor} radiusKm noFog/>
  │    │    └─ <ExploreControls>  ← WASD 移动 + 相机跟随（命令式，不触发 React 重渲染）
  │    └─ <MapErrorBoundary>  ← 本次新增
  └─ <Clock>                  古钟（独立 rAF）

ClickableLayer.tsx
  ├─ 加载 clickable.geojson：先读 IndexedDB 缓存 → 再 fetch 网络（指纹比对）
  ├─ extents        : useMemo，每个要素的「代表点 + 包围盒」（一次遍历算出）
  ├─ visibleFeatures: useMemo，迷雾过滤（focus 气泡 / footprints / noFog 三选一）
  ├─ data           : useMemo，{type, features: visibleFeatures}
  ├─ <HitLayer>     : L.canvas() 矢量层，**不可见**，只负责点击命中
  └─ <IconsLayer>   : DOM marker（<img>），视口裁剪

mapStats.ts         各层计数（只有 HUD 在用）
实现文件：Map.tsx / ClickableLayer.tsx / GameController.tsx / mapStats.ts / styles/GameController.css
```

### 3.3 关键实现细节（改动后）

| 机制 | 现状 |
|---|---|
| **移动/相机** | `ExploreControls` 的 rAF 循环，**命令式**（不触发 React 重渲染）。玩家标记用原生 `L.marker`；相机用 `map._rawPanBy()`（Leaflet 私有 API，但正是它自己拖拽在用的） |
| **相机算法** | 每帧把玩家保持在视口内圈（30% 边距）以内；**整数像素 + 小数余量累积**；`map.fire("move")` 节流 100ms；`moveend` **只在停下时发一次** |
| **图标层** | **增量更新**：`Map<Feature, L.Marker>`，只增删差异；只有 zoom / 昼夜变化才整层重画 |
| **命中层** | 只装**视口内**（pad 0.35）要素；视口移出上次渲染范围才重建；moveend 防抖 180ms |
| **迷雾** | 探索模式 = **玩家位置单点气泡**（`focus`，半径 `radiusKm`，默认 0.5km 来自后端）；**不再用 `足迹.json`**（用户明确要求） |
| **位置上报** | 每走 `max(150m, radiusKm×400)` 才 `setCursor()` 一次（不每帧） |
| **缩放** | 探索模式 `lockZoom`：minZoom=maxZoom=18，禁用滚轮/双击/触摸/框选/键盘缩放 |
| **奔跑** | 按住 Shift ×2.6；基础步速 2.2 米/游戏秒（×时钟倍率 15） |

---

## 4. 本次会话做过的改动（全部未提交）

### 4.1 保留下来的

| # | 改动 | 文件 |
|---|---|---|
| 1 | 图标尺寸随 zoom（40→64px，原固定 36） | `ClickableLayer.tsx` |
| 2 | 玩家标记：红圆点 → **圆圈包围的「梁」字** | `Map.tsx` + `styles/GameController.css` |
| 3 | **奔跑**（Shift ×2.6）+ 基础步速 1.4 → 2.2 | `Map.tsx` / `GameController.tsx` |
| 4 | 相机：死区跳变 → 每帧整数 `_rawPanBy` + 节流事件 | `Map.tsx` |
| 5 | 图标层 → **增量更新** | `ClickableLayer.tsx` |
| 6 | 命中层 → **只装视口内** + bounds 缓存 | `ClickableLayer.tsx` |
| 7 | 迷雾 → **单点气泡**，不用足迹 | `Map.tsx` / `ClickableLayer.tsx` / `GameController.tsx` |
| 8 | 探索模式**锁定 z18** | `Map.tsx` / `GameController.tsx` |
| 9 | `data` / `shownFootprints` 稳定引用（useMemo） | `ClickableLayer.tsx` / `Map.tsx` |
| 10 | `featureExtent`：代表点+包围盒一次算出 | `ClickableLayer.tsx` |
| 11 | **调试 HUD（硬编码开）+ 错误边界** | `Map.tsx` / `mapStats.ts` / CSS |
| 12 | 菜单地图加 `showAllIcons`（不启用迷雾，全部 POI 都画） | `GameController.tsx` / `Map.tsx` |

### 4.2 **已回退 / 失败**的实验（别再重复）

| 实验 | 结果 |
|---|---|
| 给 `.leaflet-tile-pane / overlay-pane / marker-pane` 加 `will-change: transform` | ❌ **直接黑屏**（瓦片放大 4 倍 + 几百 marker，强制 GPU 合成层吃满显存）。已回退并留了注释 |
| 用 `?debug=1` URL 参数控制 HUD | ❌ 用户看不到 → 改成硬编码开 + 挂 `document.body` + `position: fixed` |
| 把 HUD 挂在 `map.getContainer()` 里 | ❌ `.leaflet-container` **没有 `position: relative`**，且被 `.explore-map`(z-index 50) 的层叠上下文压住 → 看不见 |
| `?debug=1`（第一版）写在 `if (p)` 里更新 | ❌ 位置没加载时 HUD 是空的 |

### 4.3 修掉的自造 bug（血泪，务必别犯）

| Bug | 现象 | 根因 |
|---|---|---|
| **Hooks 在 early return 之后** | **白屏** | `if (!ready) return null;` 之后才调 `useMemo` → 「Rendered more hooks than during the previous render」。**两处**：`Map.tsx` 的 `shownFootprints`、`ClickableLayer.tsx` 的 `data`。已前移 |
| `panBy` 每帧调用 | 卡死 | `panBy` 内部是 `_rawPanBy` + `fire('move')` + `fire('moveend')`；而 **Canvas 矢量层监听的是 `moveend`** → 每帧全量重绘 2000+ 图层 |
| 亚像素平移 | 疑似卡顿源之一 | Leaflet 自己的拖拽是**整数像素**（鼠标坐标），传小数 `translate3d(1.37px)` 会逼合成器重采样 |
| `extraFootprints` 原地 push | 迷雾不更新 | 数组引用不变 → `useMemo` 不重算（当时加了 `footprintVersion` 版本号；现已不用足迹） |

---

## 5. 已经**排除**的假设（有证据，别再查）

| 假设 | 排除依据 |
|---|---|
| 瓦片放大 4 倍导致卡 | 用户实测：**菜单地图 z18 也流畅** |
| icon 层数量 / 重建是主因 | 用户实测：**菜单地图渲染全部 2344 个 icon 且平移也流畅** |
| 隐藏的 `move` / `moveend` 监听者 | 全项目 grep 过，只有 `ClickableLayer` 的两个层在监听 |
| 瓦片文件太大 | z16 最大 10.1 KB，中位 2.8 KB，平均 3 KB |
| 矢量几何太大 | clickable.geojson 全部要素坐标点合计只 **10819** 个（最大单要素 1426 点） |
| rAF 循环里的 JS 计算 | 每帧只有几十次浮点运算 + 几次 Leaflet 查询，微秒级 |
| `isExplored` 的 O(n×m) | 已改网格索引，且现已不用足迹 |

---

## 6. ⭐ 尚未验证的假设（下一步要查的）

### 假设 A（**用户最强线索**）：首次访问某区域的「一次性加载/绘制」开销

「**走过的地方流畅，新走的地方卡**」最直接的解释是：

- 走过的地方：瓦片已在浏览器的 HTTP/内存缓存里、图标已解码、布局稳定 → 只需重绘
- 新地方：**瓦片首次从 Vite dev server 请求 + PNG 解码 + 首次绘制**；图标首次解码

**验证方法（唯一能定案的）**：
1. F12 → **Performance** → 点录制 → 走路进入一个**没去过的区域** → 停止
2. 看 Main 线程的**长任务（红色三角）**：
   - 若是 `Decode Image` / `Resource Load` / `Loading` → **瓦片/图标首次加载**
   - 若是 `Recalculate Style` / `Layout` / `Paint` / `Composite Layers` → **DOM/渲染**
   - 若是某个 JS 函数 → 看是谁
3. F12 → **Network** → 过滤 `/tiles/` → 看新区域是否大量请求；`Size` 列是 `(disk cache)` 还是真实字节

**若确认是瓦片首次加载**，可选修法：
- **生成 z17/z18 瓦片**（只在城区范围）。代价实测：城区 ±3km 为 z17 600 张 / z18 2304 张；±5km 为 1560 / 6162（约 45 MB）；整框是 86802 / 345983（2 GB，不可行）
- 或**前端预热**：把玩家周围的瓦片在空闲时 `new Image()` 预取
- 或用 `img.decode()` 提前解码

### 假设 B：`iconSrc` 的重复 preload

`iconKeys` 由 `data` 派生 → `data` 变 → `preloadIcons(46 张)` → `setIconSrc(新对象)` → **额外一次 React 渲染**。
已用「排序后的签名」缓解，但 `iconSrc` 对象身份仍会变 → 触发 `render(false)`（增量，开销小）。

### 假设 C：`HitLayer` 尚未增量

`IconsLayer` 已改成增量，但 **`HitLayer` 仍是 `clearLayers()` + `addData()` 全量重建**（每次 `data` 变）。
虽然已限制在视口内，但一次重建几百个 `L.Path` 仍可能造成尖峰。**建议同样改成增量。**

> ✅ **已修（2026-09-16）**：`HitLayer` 重写 —— 图层只挂一次（不再依赖 `data`）、
> `Map<Feature, L.Layer>` 增量增删、`data` 变化只重新 diff 不重建。见 §11。

### 假设 D：z18 放大瓦片的「首次合成」成本

即便瓦片已下载，z18 下第一次把它们**放大 4 倍合成**可能比之后重绘贵（浏览器缓存了栅格化结果）。
这能与「走过的地方流畅」吻合。

---

## 7. 调试工具（本次已备好）

### 7.1 性能 HUD（**已硬编码为开**）

```tsx
// Map.tsx:37
const DEBUG_HUD = true;      // 诊断完请改回 URL 开关
```

左下角（`position: fixed`，挂 `document.body`，z-index 99999）：

```
FPS 60   最差帧 8ms   z18   跑:否
玩家 32.39412, 119.43650
图标 412  命中 380  可见 1204
命中重绘 3.2ms  图标重绘 18.4ms
```

| 字段 | 含义 | 来源 |
|---|---|---|
| `FPS` | 每 500ms 统计 | `ExploreControls` 的 rAF |
| `最差帧` | 最近 500ms 内最长的一帧（ms） | 同上 |
| `图标` | 当前 DOM marker 数 | `IconsLayer` |
| `命中` | 命中层当前要素数 | `HitLayer` |
| `可见` | 迷雾过滤后的要素数 | `ClickableLayer` |
| `命中重绘` / `图标重绘` | 两层上一次重建耗时（ms） | 同上 |

**只有探索模式有 HUD**（它在 `ExploreControls` 里创建）。

### 7.2 错误边界

`Map.tsx` 里的 `MapErrorBoundary` 会在崩溃时把错误栈**显示在屏幕上**（而不是一片黑）。
排查白屏/黑屏时优先看这里，再看 F12 Console。

### 7.3 逐层开关（✅ 已实现，2026-09-16）

URL 加参数即可，**一次只关一层**：

```
?noicons=1   关掉图标层（DOM marker）
?nohit=1     关掉命中层（Canvas 矢量层）
?nopan=1     关掉相机跟随（人照走，镜头不动）
?nofog=1     关掉迷雾（全部要素可见；等同 noFog）
```

例：`http://localhost:5173/?noicons=1`（可叠加）。左下角 HUD 会显示当前关了哪些
（`关:图标/命中/相机/迷雾`）。实现：`Map.tsx` 顶部 `NO_ICONS / NO_HIT / NO_PAN / NO_FOG`。

---

## 8. 注意事项 / 别再踩的坑

1. **不要再给 Leaflet 的 pane 加 `will-change`** —— 直接黑屏（见 4.2）。
2. **Hooks 绝不能放在 early return 之后** —— 直接白屏（见 4.3）。改动后请全文件搜一遍 `useMemo|useEffect|useState|useRef|useCallback` 是否都在 early return 之前。
3. **改动 hook 顺序后必须硬刷新** —— HMR 会失效。
4. **`_rawPanBy` 是 Leaflet 私有 API**（`@types/leaflet` 里没有，需要 `as unknown as` 强转）。它正是 Leaflet 自己拖拽在用的，安全；但**不要**改回每帧 `panBy`。
5. **`cursorTrailRef` 那种"原地 push"的数组**，若下游用 `useMemo`，必须有版本号才会重算。
6. **迷雾曾经依赖 `足迹.json`**，用户明确要求**不要用**（改成玩家附近气泡 `focus`）。旧代码路径（`buildFootprintGrid` / `isExploredFast` / `footprintVersion` / `extraFootprints`）还留着，但探索模式已不走。
7. **菜单地图的 `showAllIcons`** 是用户明确要求的功能（全部 POI 都画），别当成 bug 删掉。
8. ~~本次所有改动**未提交**。~~ → 已提交到 `57da480`；之后的修复见 §11，请用 `git diff` 看。
9. 用户的耐心已经消耗很多。**先测量、再改**，不要连续盲改。

---

## 9. 建议的下一步（2026-09-16 更新）

1. ~~别改代码，先测~~ → ✅ 已测，主因已定案（见 §0）。
2. ~~顺手加 7.3 的逐层开关~~ → ✅ 已实现（见 §7.3）。
3. ~~按结论修~~ → ✅ **HitLayer 重建已改增量**；**图标兜底圆点 bug 已修**。
4. **剩余**：`?nopan=1` 仍稍有掉帧 → 怀疑**瓦片首次加载 / 首次放大合成**（假设 A/D）。
   可选：前端预热（`new Image()` / `img.decode()`）或生成城区 z17/z18（±5km ≈ 45MB）。
5. **收尾**：把 `DEBUG_HUD` 改回 URL 开关，并清理第 8 节第 6 点提到的足迹死代码。

---

## 10. 相关文件速查

| 文件 | 作用 |
|---|---|
| `trpg-client/src/in-game/Map.tsx` | 地图容器、相机、WASD、玩家棋子、HUD、错误边界 |
| `trpg-client/src/in-game/ClickableLayer.tsx` | geojson 加载、迷雾、命中层、图标层 |
| `trpg-client/src/in-game/GameController.tsx` | 三模式状态机、把 props 传给 GameMap |
| `trpg-client/src/in-game/mapStats.ts` | HUD 计数（新） |
| `trpg-client/src/styles/GameController.css` | `.explore-map` / `.player-token` / `.map-debug-hud` / `.map-error` |
| `trpg-client/vite.config.ts` | 给 `/tiles/` `/mapicons/` 加 `Cache-Control: immutable` |
| `trpg-map/` | 地图数据生成（瓦片/点击层/空间库），**本次任务不需要动** |

---

## 附：本次会话还顺手做完的、与性能无关的事（供了解，别重复做）

- 地图数据规范化：所有数据收到 `trpg-map/数据/`，中文命名（`扬州_OSM精简.json` / `扬州_布点锚点.json` / `扬州_南宋世界.json` / `扬州_POI.json`）
- 新增 `trpg-map/城市.py`（城市画框唯一真相源）、`trpg-map/生成.py`（一键链路）、`trpg-map/冻结POI.py`
- `trpg-map/cut.py` 退役；`pbf_to_json.py` 加 `--city/--bbox`（按画框裁剪）
- 修 `classifier.py` 不认 `natural=wood` 导致林地大面积丢失
- `build_world.py` 把水道中心线按真实宽度 buffer 成面（长江 1500m），并支持 POI 冻结表防名字漂移

---

## 11. 定案记录（2026-09-16）

### 11.1 卡顿主因：`HitLayer` 每次 `data` 变就整个重建 ✅

`ClickableLayer.tsx` 的 `HitLayer` 重写：

| 之前 | 现在 |
|---|---|
| `useEffect([map, data, extents])` — 迷雾一变就拆掉整个图层重建 | `useEffect([map])` 图层只挂一次；`data`/`extents` 走 `ref` |
| `L.geoJSON(全部)` 一次 `clearLayers()+addData()` | `Map<Feature, L.Layer>` 增量增删（只加新进视口、只删离开视口） |
| `L.canvas()` renderer 每次重建 | renderer 只建一次，所有 path 共用 |
| — | `render(mode)`：`"viewport"`（可短路）/ `"data"`（内容变了必重算）/ `"clear"`（整层重画） |

### 11.2 图标变兜底圆点的 bug ✅

`IconsLayer.getIcon` 的缓存键 `key|size|night` **不含 URL**。迷雾开着时 `data` 频繁变，
某帧新图标还没 preload 完 → `src[key]` 是 `undefined` → 画成兜底圆点并被**缓存**；
之后真图到了也一直返回缓存的圆点。nofog 时一次到位所以正常（与用户观察一致）。

修法：① 缓存键加 url（`key|size|night|url`）；② 已渲染 marker 发现 icon 变了就
`setIcon()` 就地替换（增量，不整层重画）。

### 11.3 逐层开关 ✅

`Map.tsx` 顶部 `NO_ICONS / NO_HIT / NO_PAN / NO_FOG`（读 URL），传给 `ClickableLayer`
`hideIcons/hideHit` 与 `ExploreControls` `noPan`；HUD 显示当前关项。

### 11.4 验证

`npx tsc -b` + `npm run build` 全过。
