# TODO — 待办规划与开放问题

> 架构决策与当前状态见 `README.md`（原 `ARCHITECTURE.md` / `STATUS.md` / `交接-地图性能.md` 已并入）。
> 本文件只放**待办、未定问题、交接遗留**。

最后更新：2026-09-19（养成系统 v1 + 五行神庙 + 城门改造 + 前端「详细」页）

---

## ⚑ 当前优先级（交接用）

> **本轮新完成**（2026-09-20）：**技能树**（正典 `技能树.json` + 运行表 `招式表.json` + `skill_tree.py` + 技能点 + 前端银河/五神弧/白珠链）；
> 五行熟练度改**战斗伤害系数**；`/facility` 背景改**后端确定性映射**（删 `/facility/ui`）；相机**玩家居中 + 禁拖**；存档「传闻≠事实」。
> 上一轮（2026-09-19）：养成 v1 + 五行神庙 + 城门 + 详细页（§16.18）。**详版见 `交接.md`**。
>
> `history` 滑动窗口/摘要：模型上下文足够大，**暂缓**。

1. **技能树内容/数值调参** —— `技能树.json` 22 节点是**草稿**待用户改（招名/数值/前置/要求）；五行伤害系数（每点 +1%）、技能点概率（1%/2%）、曲线。
2. **五神具体设定 + 神像美术** —— **（计划 2026-09-21）**样貌 / 性格 / 生日 / 神职 / 庙祝 / 传说，定稿写 `trpg-world/` 并同步 设施/技能树/神像；五张线稿已接入（`src/assets/神明/`，**assets 不入 git**），可迭代。做图流程见 `五行/ai/README.md`。
3. **详细页四样式（`n1..n4`）+ 文字颜色** —— 活动名颜色、养成/自由小印，用户尚未拍板。
4. **战斗时间折算接入** —— `clock.begin_battle/end_battle` 已就绪，但战斗流程还没接。
5. **世界地图 / 跳城旅行** —— 跨城靠叙事或渡船；真做需要世界级地图（见 §8.3）。
6. **环境事件层**（替代已停用的随机骰子）—— 见 §十四。
7. **战斗路线 B（真战棋）** —— 见 §四。
8. **地图性能残余**（`?nopan=1` 仍稍有掉帧，见 §十二）。
9. 大工程**未动**：**信息边界（§九）**、**知识库层（§十）**。

> 另：**大跨度时间流逝规则**（`总览.md`「住店≠睡到天亮」）已有 `sleep` 工具 + 规则 14，
> 但未专门补「住店只是订房」那条——规则热更，需要时补。

---

## 〇、已完成（挪出待办）

- ~~**`tools/` 重组为 4 个包**~~ ✅（2026-09-19）：`大模型(26)/小模型(6)/导演(1)/核心(16)`；
  43 个 `.py` import 改写 + 21 处 `__file__` 路径深度修正；依赖分层无环。见 `交接.md` §一。
- ~~**时间工具拆分**~~ ✅：新增 `advance_time(ke/shichen)`；`update_time` 收窄为“直接设置”。
- ~~**金钱账本 + 只扣一次**~~ ✅：`金钱.json.最近支出`（最近 3 笔）；台词/场外机械护栏禁止一切改状态工具。
- ~~**「探索」按钮确定性**~~ ✅：玩家**自主切换**（`engine.enter_explore()`，纯前后端逻辑，不经 GM）；`resume_exploration` 工具退役（`TRPG_RESUME_TOOL=1` 回滚）。
- ~~**探索地图通用 BGM**~~ ✅：叙事→探索时小模型从无绑定通用曲里挑（默认 `山中好岁月`）。
- ~~**总览图迷雾**~~ ✅：改按已探索足迹过滤（删 `showAllIcons`）。
- ~~**按钮悬停**~~ ✅：`未选中时按钮.png` / `金红矩形按钮.png`（11 按钮 + `.sm-toggle`）。
- ~~**背景转场预加载**~~ ✅：`BackgroundTransition` 先 `decode()` 再交叉淡化。
- ~~**大模型输出 JSON 容错**~~ ✅：`parse_instruction_array`（围栏/前后废话/未转义英文引号）。
- ~~**场外话（OOC）规范**~~ ✅：规则 + `OOC_NOTE`（现代白话直答，不入戏）。
- ~~**岳阳“陆地上的船”修复**~~ ✅：`修复水域POI.py` 吸附/删除 + 生成器加固（edge 池去水类 + `_repair_water_pois`）。
- ~~**轻功→步速 / 奔跑耗精力**~~ ✅：`移动.json` + `GET /state` 下发 / `POST /run` 结算。
- ~~**导演接《故事大纲》**~~ ✅：system 材料 + 首局兜底 + 剧透机械兼底；诊断 `director_probe.py`。

- ~~**多城市（扬州 / 岳阳）**~~ ✅（2026-09-18）：地图资产按 `map_id` 分目录（瓦片/点击层/碰撞层/空间库）；
  **游戏城市由玩家坐标推导**（看别的地图零副作用）；前端搜索框跨城搜地点 + 回车跳转；
  `城市.py` 为单一真相源；`生成.py --city` 一键链路。见 README §13.5 / §16.16。
- ~~**岳阳地图**~~ ✅（2026-09-18）：源用 `湖南.pbf` 全量 → **完整洞庭湖 1078 km²**（231/231 way）；
  君山岛（保留 `place=island`）；**锦香宫**建筑群 550×400 m（330 亩）+ 锦香阁 + 竹廊 + 果林 + 泊船处；
  `palace` 图标；全岛「君山茶园」。
- ~~**地形步速倍率**~~ ✅（2026-09-18）：`export_walkable.py:TERRAIN_RULES` → `terrain` 层；
  林地 ×0.6 / 滩涂 ×0.45 / 山地 ×0.35；前端 `terrainAt()` + 徽章。见 README §13.6。
- ~~**瓦片画建筑**~~ ✅：`should_draw_building()` 原本直接 `return False`，宫殿根本不上图；
  已新增 `renderer._draw_buildings`（民居仍不画，扬州输出不变）。
- ~~重构 LLM 返回结构（背景工具化）~~ → 已完成：`bg` 类型删除，背景改走 UI 事件 `kind:"bg"`。
- ~~存档流程重写~~ → 已完成（`save_pipeline.py` + `POST /save`）。
- ~~工具双份登记~~ → 已合并为 `tools/大模型/registry.py`。
- ~~记忆库两 collection~~ → 已完成（`gm_memory` / `char_memory`）。
- ~~世界状态 + 日推演 worker + 宏观时间线~~ → 已完成（宏观 89 条，1220~1279）。
- ~~UI 事件管线（⑧）~~ → 已完成：`bg`/`music` 由小模型选（`tools/小模型/ui_sim.py`，双清单+绑定+默认曲）。
- ~~时间加「刻」+「继续」刻数按钮~~ → 已完成（1 时辰=8 刻；0=只看信息）。
- ~~金钱请款确认 + 账目~~ → 已改为**直接落账**（`tools/大模型/money.py`）；请款/确认环节删除。
  （后加回**极简账本**：`金钱.json.最近支出` 只记最近 **3 笔扣费**，防同一笔重复扣。）
- ~~行动 / 台词分离（`say` 模式）~~ → 已完成。
- ~~城池内外判定~~ → 已完成（`map_query.city_context`）。
- ~~规则文件热更新~~ → 已完成（`engine._rules_text`，改 `主持人/*.md` 免重启）。
- ~~动态档案为主 + LRU~~ → 已完成：`update_character_archive` 写动态档案（模板 §9–§13），§10 超 20 条淘汰最旧→`char_memory`；存档不再直写 `char_memory`。
- ~~总纲第 5 条「硬事实由代码裁决」~~ → 已写入 README 核心原则。
- ~~地点见闻（`place_notes`）~~ → 已完成；`update_place_note` 存档专用，`query_nearby`/`query_place` 合并返回。
- ~~建筑内部结构（`place_structures`）~~ → 已完成；`update_place_structure` 存档专用；存档附加任务自动确定本局进过的建筑。
- ~~方位注入（八方位 / 城区方位）~~ → 已完成（`map_query.bearing_name` / `city_context.城区方位` / `update_location.移动方位`）。
- ~~前端去 `data/` + 势力后端化~~ → 已完成（`trpg-world/势力介绍.json` + `GET /factions`）。
- ~~难度设置 + 归隐退出~~ → 已完成（`tools/大模型/difficulty_settings.py` + `GET`/`POST /settings`）。
- ~~数据目录迁移 / 日志重命名~~ → `游戏数据`/`天气数据`/`归档存档` 移到 `trpg-server/`，日志统一 `current.jsonl`。
- ~~连续时钟 v0.2~~ → 已完成：`tools/核心/game_clock.py`（单一「游戏秒」+ 三态 + 惰性求值 + 南宋全年号/干支）、
  `tools/核心/time_flow.py`、`tools/核心/derived.py`、前端**古钟 HUD**；`/clock` 路由；`时间影响.json` 调参。
- ~~三模式状态机（探索 / 叙事 / 战斗）~~ → 已完成：探索 = 整屏 zoom18 地图 + WASD；进叙事由玩家输入（带坐标）、退叙事由主持人（`resume_exploration`）。
- ~~宏观时间线扩充~~ → **89 条（1220~1279）**。（当时的 `ensure_timeline` 增量合并**已在 README §16.14 重构中删除**：改为只读剧本现切窗口。）
- ~~修复：模拟战泄漏 / file_tools 摘除 / `state_manager` 并发写崩溃 / 背景误判乡村~~ → 均已修（见 README §16/§17）。
- ~~地图性能主因~~ → ✅ 已完成（`HitLayer` 改增量；图标缓存 bug 已修；逐层开关已实现）。残余见 §十二。
- ~~地点交互（进入/观察/回忆）+ 饥饿 100 制 + 城门限时 + 探索硬约束~~ → 已完成（最近提交）。
- ~~存档蒸馏加固~~ → 已完成（12 轮上限 + 240s 看门狗 + `audit.log`；`SAVE_TOOLS` 收窄为白名单）。
- ~~内容生成型骰子（`daily_event_dice` / `travel_event_dice`）摘出 LLM 工具集~~ → **Step 0 已完成**（2026-09-17，见 §十四）。
- ~~人物表情改由小模型填充~~ → 已完成（2026-09-17）：新增 `tools/小模型/expression_sim.py`（大模型不再输出 `expression`）；
  `特定人物.md` 已删、`check_expression` 摘出 LLM；验证脚本 `verify_expression.py`（一次性，可留作回归）。
- ~~NPC 唯一姓名策略~~ → 已完成（2026-09-17）：运行时 `总览.md` 规则 16 + 存档流程 §三 +
  `save_pipeline.unnamed_characters`（**不再要求两局都出现**）。**遗留**：既有代称档案/数据待迁移（见 §十一）。
- ~~世界状态注入剧透（定时线全量未来）~~ → 已修（2026-09-17）：宏观时间线改为**只读剧本 + 按日期现切窗口**
  （不再拷进档、不记「已触发」）；档内只留 `世界线程`（NPC 线程）。见 README §16.12 / §16.14。
- ~~宏观时间线`text`太简，扩写 + 叙事字段注入~~ → 已完成（2026-09-17）：89 条各加 `辐射范围`/`具体影响`/`江湖反应`/`GM叙事参考`；
  随 `预兆`/`宏观` 一起注入（README §16.13）。
- ~~导演（GLM）落地~~ → 已完成（2026-09-17）：`tools/导演/director.py`，**每局一次**异步产出《导演简报》
  常驻注入（v4 提示词：强制取舍 + 防漏自查 + 查主角行动）；见 README §5.9 / §16.15。

---

## 一、世界推演收尾（⑦ 剩余）

- [x] **活跃人物名单自动化** ✅：存档管线（`save_pipeline.contacted_characters`）用三个确定性来源
      抽取本局接触过的人物（`chat` 发言人 / `get_character` 解析名 / `char_memory.owner`），
      经 `world_threads.promote_active` 写入 `活跃/<名>.md`（幂等；曾淘汰者自动迁回）。
      **所有角色地位平等**：新登场的 NPC 也建档（静态 §1–§8 + 动态 §9–§13）。
- [x] **`get_character(name)` 工具** ✅：NPC 登场时一次读全**静态正典 + 动态近记忆**。**深层记忆不由它取**——由 LLM 按需调 `DB_query_tool(collection=char_memory, owner=…)`。
- [ ] **活跃 → 不活跃 淘汰规则**（LRU？按出场频率？）—— 🅿️ **搁置**：等活跃 NPC 真正增多、出现体积/噪音问题后再定。
- [x] **世界线程体积与注入策略** ✅：`world_threads.threads_view()` 按 `纳入上下文` 裁剪；宏观**不落档**，由 `macro_timeline.view(今天)` 现切窗口（见 README §16.12/16.14）。
- [x] **时间权威** ✅：规则写入 `总览.md` 通用规则 10；`engine._check_time_authority` 机械兜底。
- [ ] **`_check_time_authority` 换语义**：连续时钟后，从"推进了没落库"→"**大幅跳时**却没落库"。

---

## 二、UI 事件管线（⑧）✅

- [x] **`ui_event` 的 kind 清单与协议** ✅：`bg` / `music` / `minigame`（`tools/核心/ui_events.py` + 前端 `types/gametype.ts`）；后续新增 `mode` / `battle`。
- [x] **把 `bg` 从大模型提示词迁到小模型** ✅。
- [x] **背景音乐（BGM）** ✅：`kind:"music"`；小模型选曲（`音乐清单.md` / `战斗音乐清单.md`）；循环播放。
- [ ] **小游戏事件类型与暂停/恢复语义**（`minigame`）。
- [ ] **前端浮层结构**（`MiniGameHost` + 懒加载注册表）。

---

## 三、内容层（⑥）

- [ ] **现有 55 份静态档案逐步对齐模板**（`档案模板/静态模板.md` §1–§8；动态内容迁往 `活跃/<名>.md`）。
- [x] **`GET /factions` 投影** ✅（2026-09-13）：`trpg-world/势力介绍.json`（已剔除剧透，人工维护）；后端 `tools/大模型/factions.py`；前端画廊改 fetch。

---

## 四、战斗系统 — 路线 B：深化为「真战棋」（v0.2+，已定方向）

> 定位：**TRPG 主干 + 战棋战斗模块**；战斗往「真战棋」深化。

- [ ] **地形 / 障碍**：墙、桌椅、水、高低差、掩体、困难地形（影响移动/射程/视线/命中）——已初步有 `blocked()`/河/房/树，待扩展。
- [ ] **寻路 + 移动预览**：障碍连成可寻路图（Dijkstra/A*）；前端高亮可达格与路径。
- [ ] **借机攻击 / 控制区（ZOC）**
- [ ] **目标多样化**：歼灭 / 护送 / 坚守 / 夺旗 / 斩首 / 存活 N 回合
- [ ] **技能体系**：NPC 按档案配 `招式`；玩家多行招式 / 融合技
- [ ] **养成 / 装备**：`update_ability` / `train_skill`；武器 / 防具 / 道具
- [ ] **更多数值维度**：暴击 / 格挡 / 护甲 / 破甲 / 命中闪避细分
- [ ] **战果写回世界线程 / 世界推演**：伤亡 / 俘虏 / 战利品 / 名声（接 `update_money`、背包、世界推演）
- [ ] **视野 / 战争迷雾**、**先手 / 行动条**（可选）；更大 / 多层地图
- [ ] 玩家作为**受击方**时的五行判定（现只在施放方结算）；扇形 / 直线范围
- [ ] **战斗时间折算接入**（见 ⚑ 优先级 2）

---

## 五、小游戏系统（⑨）

集成小游戏，可直接在前端游玩（统一策略：**逻辑搬后端 + React 重画 UI，结果回流游戏状态 + LLM 叙事**）。

### 可复用现成实现（`C:/Users/20866/Desktop/TRPG2/tools/`）

| 文件 | 玩法 | 迁移要点 |
|------|------|---------|
| `围棋.py + gnugo.exe` | GNU Go 引擎（GTP 协议），5 档难度 | `GnuGoAI`/`Board` 类纯逻辑可搬后端，GUI 用 React Canvas 重写 |
| `投壶.py` | 力度 + 角度双阶段控制 | 状态机逻辑可搬后端 |
| `斗蟋蟀.py` | 蟋蟀自动对战 + 下注 | 最轻，直接挂 `modify_money`，**建议先做它打通链路** |
| `科举.py + 科举题库.json` | 答题 | 题库现成 |
| `生成天气.py` | — | 已迁移为本项目天气系统 |

- [ ] 象棋（无现成实现，需规则库/引擎）
- [ ] 赌博（本质是"下注模块"，可复用给斗蟋蟀）

---

## 六、属性修改 / 养成工具

- [x] **上限派生已做**（2026-09-15）：`tools/核心/derived.py` —— **体力 = 生命值上限、內力 = 精力值上限**；
      `属性.json` 为真相源，`状态.json` 的上限由 `sync()` 对齐。
      睡眠恢复量 = 內力 ÷ 睡眠回满时辰，养成提高內力后自动变强。
- [x] **养成写入接口** ✅（2026-09-19）：`tools/核心/growth.py` 管道（`gain` / `add_buff`；点数 ×buff ×曲线 → `属性.json`，buff 存 `加成.json`）；工具 `use_facility` + 接口 `GET /facility`（选项 + 背景映射，不调模型）；设施表 `facilities.json`；成长曲线 `成长.json`；战斗成长 `battle_session._grant_battle_growth`；五行跨 `招式表.解锁` 阈值自动解锁（`derived.sync()` 已接）。**注**：**不做独立 `train_skill`**——成长只由「**设施 / 战斗**」产生（用户定）。
- [x] **技能树 + 技能点** ✅（2026-09-19）：招式表搬 server 并清空（运行表）；`trpg-server/技能树.json` 正典（全部可学招式 + 前置/要求/花费）；`工具 核心/skill_tree.py`（`learn`/`view`/`roll_point`）；技能点（设施 1% / 战斗 2%）；点亮同步写 `属性.json` + `招式表.json`；熟练度改作伤害系数（每点 +1%）；`growth._unlock_moves` 自动解锁已删；接口 `GET /skilltree` / `POST /skilltree/learn`。**待**：无（前端 UI 已做）。
- [ ] **事件图打磨**：34 张 `public/eventicons/*.png`（`icongen/gen_event_icons.py` 生成）；提升辨识度。
- [x] **技能树前端** ✅（2026-09-19）：`in-game/SkillTree.tsx` + `styles/SkillTree.css`——菜单「技能树」→ 银河（`Galaxy.tsx`，不监听鼠标）→ 五神圆弧（SVG 五扇区 + 手绘剪影）→ 班级树（白链条 + 三态）；点节点出详情卡 + 点亮。零大模型。
- [x] **轻功 → 移动速度** ✅（基础版）：`移动.json` 的 `轻功每点步速` → `GET /state` 下发 → 前端步速 = 基础步速 ×（1 + 轻功×系数）。
- [x] **奔跑耗精力** ✅：只对奔跑「超出步行的那段距离」按米扣（`POST /run`）；避免与时间流逝双重计费。
- [ ] **轻功作为技能**（进阶）：仍需技能体系能表示 / 升级轻功（现只是属性）。

---

## 七、日历系统

游戏内历法：**新历（公历）＋农历并行**，作为「时间」的权威换算层。

- [x] **年号 / 干支 / 中文月日（南宋全表）** ✅（2026-09-15）：`game_clock.ERAS`（1127~1279，22 个年号）+ `era_name` / `ganzhi_year` / `chinese_date`；写入 `基本信息.时间.纪年`/`干支` 并进 `/clock` 锚点。
      **注意**：目前只是「年号 + 公历月日的中文写法」，**尚未做农历换算**。
- [ ] **双历并行的数据结构**：时间现已是「单一游戏秒 + 派生投影」，但尚未做**农历**（朔望/闰月）与公历的并行换算。
- [ ] **农历与节气**：二十四节气、传统节日（元宵/端午/中秋/除夕…）、朔望月相——用于市集、庙会、灯会、祭祀等定时活动。
- [ ] **与既有系统的联动**：天气（按节气/季节）、世界推演（宏观时间线与 `模拟游标` 统一）、环境事件（按节日给权重）。
- [ ] **时间工具升级**：`advance_time` 目前支持 `ke`/`shichen`（推进）；后续可支持「过 N 天 / 到某日」等表达并自动换算农历。
- [ ] **前端显示**：「嘉定X年X月X日（农历…）· 节气 · 时辰」。
- [ ] **天气按年维度**：现为单张 366 天表（1220），跨年只是复用。
- 注：历法**不绑定真实日期**，是纯游戏内时间。

---

## 八、地图内容扩展

### 8.1 地图数据库加 `description` / `type`

- [x] **`features` 表加 `description`** ✅。
- [x] **动态见闻 `place_notes` 表** ✅（建库重建**不删**）；`update_place_note`（存档专用）。
- [x] **建筑内部结构 `place_structures` 表** ✅；`update_place_structure`（存档专用）。
- [ ] **`type`（语义分类）**：区别于现有 `category`/`ancient_kind`，待做。
- [ ] **定时事件**：`description` / `place_notes` 记录「xx 广场在 xx 时刻有烟火表演」这类**时空化描述**，与日历联动。
- [ ] **静态初值产出**：`build_world.py` 从 OSM tag / 命名池生成 `description` 初值。
- [ ] **补史实城门 / 水道**：现只有 7 座城门（「水门」在城**东北**），**无「西水关」**等史实水门；实战中 GM 因地图无「西水关」而借把「水门」当西水关。需据史实补全（改地图源 → 重建空间库）。

### 8.2 地图特色重制（地域差异化）

- [x] ~~**把 `build_world.py` 里扬州的 LAYOUT 参数抽成城市配置**~~ ✅（2026-09-18）：
      **`CITY_CONFIGS`**（投影中心 / 老城 bbox / 坊名池 / 街巷改名 / 史实锚点 / 城外聚落 / **地名改名**）。
      ⚠️ **扬州条目不许改**：SEED=42 下输出必须**逐字节不变**。
- [x] ~~新增城市要改代码~~~~ ✅：`城市.py`（`map_id` / `pbf` / `start`）+ `生成.py --city`。
- [ ] **问题仍在**：**城区算法本身还是同一套**（坊巷棋盘 + 同种 POI 池）→ 各城长得像（只是参数不同）。
- [ ] **引入「城市风格参数 / 模板」**：江南水乡 / 北方中原 / 山地关隘 / 沿海港口。
- [ ] 可参数化维度：路网形态、坊巷间距、建筑密度与朝向、水系与植被、坊名/POI 词表、城墙形制、色彩与瓦片样式。
- [ ] 与 `song_kinds.py` 的 zone/kind 词表结合。

### 8.1.1 建筑群（宫观 / 府邸）✅

- [x] `build_world.build_compounds()`：读 `数据/<城市>_建筑群.json` → 院墙 / 殿宇 / 廊庑 / 园圃 / 泊船处。
- [x] 首个用例：**锦香宫**（君山岛，550×400 m / 330 亩，中轴三进 + 锦香阁）。
- [ ] 布局模板只有 `palace`（中轴三进）；可补 `temple` / `manor` / `village`。
- [ ] 院落内部**细节**（正殿台阶 / 月台 / 水池 / 门阀）靠瓦片图形表达，目前只有轮廓+屋顶。

### 8.3 世界地图

- [ ] **多地图支持**：`maps(map_id, name)` 已预留；**扬州/岳阳两城市已共享一套多城机制**（资产分目录、前端搜索切图），
      但**尚无世界级地图**（位置、距离、官道/水路、旅行耗时）。
- [ ] **分层缩放**：世界地图 ↔ 城市地图，前端可切换/缩放。
- [ ] **旅行机制**：跨图移动消耗时间与精力 —— 现在能做**“叙事式跳城”**
      （`update_location` 带非步行 move_mode，如渡船；**步行/探索会被硬闸拦住**），但无路途遭遇/耗时结算。
      → 接环境事件 / 世界推演与日历。
- [ ] **数据来源**：史实南宋行政区划 / 驿道 / 水路骨架 + 程序化补地名。

### 8.4 地图点选目标 + 行程耗时（以「刻」为单位）

> 目标：点地图任意一点 → 告诉 LLM「要去这里」→ 后端算路程、耗时（刻）→ LLM 推进时间。

- [x] ~~**前端点选**~~（探索模式已可 WASD 走到任意点）。
- [ ] **路网寻路（后端）**：把目标点吸附到最近路网节点，沿街道网络算实际步行路程（非直线）——需把坊巷/官道几何连成**可寻路路网图**（Dijkstra/A*）；现有 `query_nearby` 只有几何，未成图。
- [ ] **时间以「刻」计**：路程 ÷ 速度 = 刻数，四舍五入。1 时辰 = 8 刻。
- [ ] **速度模型**：体力（饥饿/伤势/健康）→ 速度系数；~~轻功（属性/技能）→ 速度系数~~ ✅ 轻功系数已做。
- [ ] **推进时间**：后端把「目的地/路程(米)/耗时(刻)」交给 LLM，LLM 调 `advance_time(ke=N)`。
- [ ] **落库一致**：`update_location` 落到目标，并与行程耗时一致。
- [ ] **与 `总览.md`「叙事节奏与移动」对齐**：现有规则按「时辰」太粗，改**以刻推进**。
- [ ] **依赖**：轻功需有技能/属性系统（见 §六）。

---

## 九、信息边界（NPC 全知 / 上下文泄漏）🚧

> 架构决策见 `README.md` §5.7。

- [ ] **已定方案：两段式调用**——Pass 1 GM 出叙述 + 舞台调度（不产 NPC 引语）；Pass 2 每个开口 NPC 用**过滤后的知悉集**单独出对白；合并事件流。
- [ ] **`current.jsonl` 加知情字段**：`在场` / `知悉` / `公开度`（私密·传闻·公开）/ `scene_id`。
- [ ] **知悉集拼装**：`在场∋X` ∪ `知悉∋X` ∪ 公开传闻 ∪ `char_memory[owner=X]`；`build_messages` 提供"角色视角块"。
- [ ] **tag 来源**：小模型异步标注（倾向）或大模型 `scene` 控制事件。
- [ ] **传播**：`公开度` 随 `world_sim` 日推演演化（私密→传闻→公开）。
- [ ] **校验**（可选）：小模型核对 NPC 台词是否引用知悉集外事实。
- [ ] 统一 `visibility` 语义：`char_memory.owner` / `turns` 知情字段 / `lore.visibility` 三层对齐。
- [ ] **足迹 → 在场追踪**（最省事的切入点，见 README §8.5；须先降采样）。

---

## 十、知识库层（正典检索）🚧

> 架构决策见 `README.md` §5.8。与记忆层性质相反：md 是真相源，chroma 是可重建投影。

- [ ] 新建 `trpg-world/考据/`，整理史实/日常**事实卡片**（出处：梦粱录、武林旧事、《蒙元入侵前夜的中国日常生活》）。
- [ ] `trpg-db/chroma_lore/` + `ingest_lore.py`（独立于 `init_GM_DB --reset`；文件 hash 增量；`--reset` 重建）。
- [ ] 单 collection `lore` + metadata：`source`/`domain`/`era`/`visibility`/`confidence`。
- [ ] chunk：按标题切 + context 前缀；单块 200–500 字。
- [ ] `KB_query_tool`（只读、带出处）注册进 `registry.py`。
- [ ] 引擎每轮**预检索注入**（规则层仍全量；正典只走检索）。
- [ ] 铁律：KB 结果不进记忆；无命中必须承认、不得编造。
- [ ] 先索引现有 `trpg-world/` 正典跑通（顺带解决"正典层检索接口未做"）。

---

## 十一、其他

- [ ] **⭐ `history` 滑动窗口 / 摘要（优先）**：长局会把大模型上下文撑爆。可先"保留最近 N 轮 + 更早摘要"；
      原则：等记忆系统确实接住关键信息后再做（避免逻辑断裂）。**用户目前暂缓。**
- [ ] **大跨度时间流逝规则**：`总览.md` 补"住店 / 投宿只是**订房**，不等于睡到天亮；过夜须玩家明说"（规则热更，免重启）。
- [x] ~~**混乱值评估自动化**~~ → **已废弃**（2026-09-18）：`混乱度.json` 与「混乱值」系统整体删除，不做了。
- [ ] **放弃确认换成自定义浮层**（替换 `window.confirm`），与国风 UI 一致。
- [ ] **瓦片文字标注**（需中文字体）；城墙史实微调。
- [ ] **旧「代称」档案/数据迁移**：既有用职业代称建档的 NPC（如「元熙布行掌柜」「怀茂青楼老妇」「怀顺茶坊老板」「桂安酒肆妇人」「灶房汉子」「挑炭人」）
      按新策略改成**正式姓名**，并同步 `角色静态/动态档案`、`世界线程.json` 人物线程、`char_memory.owner`、`gm_memory`、`current.jsonl`（可写一次性迁移脚本）。
      ⚠️ **注**：这批正是 test 产物，已随测试档**删除**（见 `trpg-server/删档.md`）；本项仅作今后同类迁移的预案。
- [ ] **自动删档**：把 `trpg-server/删档.md` 的清单/规则写成脚本（一键丢弃本档、保留正典与跨档内容）。
- [ ] **导演增强**：① 用**代码侧线索台账**对简报做 diff（防它自查时“悄悄漏”）；
      ② 主线选择会飘（同输入两次可能不同）→ 固定择线规则或跑两次取交集；
      ③ 输出常超 500 字 → 加更硬的截断。
- [x] **导演接《故事大纲》** ✅（2026-09-18）：`tools/导演/director.py` 把 `trpg-world/故事大纲.md`
      作为 system 材料随存档一起喂；**GM 仍永远看不到大纲**，靠《导演简报》间接接收方向。
- [x] **首局无前情兜底** ✅：`previous_story` 为空时改用**当前状态 + 开局设定**当材料
      （`_first_material`），否则首局导演永远不跑、开场没钩子（旧增强项 ④）。
- [x] **剧透兼底（代码）** ✅：`scan_spoilers` + `sanitize`——命中底牌词先重试改写，仍漏则机械消弧；
      诊断脚本 `director_probe.py`（`--e2e` / `--no-outline`）。
- [ ] **手柄支持（Gamepad API）/ 语音输入（STT → `/action`）**；输入抽象层（`in-game/input/`）。
- [ ] **类型体系**：`types/gametype.ts` 补时钟 / 模式类型。

---

## 十二、地图性能残余 + 清理

> 主因已定案修复（见 `README.md` §13.4）。

- [ ] **`?nopan=1` 仍稍有掉帧** → 怀疑**瓦片首次加载 / 首次放大合成**。可选：
      前端预热（`new Image()` / `img.decode()`）或生成城区 z17/z18（±5km ≈ 45MB/城）。
      ⚠️ **多城市后瓦片体积翻倍**（岳阳 z16 就有 43278 张）；若真要 z17/z18 得先看磁盘。
- [ ] **收尾**：把 `DEBUG_HUD` 从硬编码改回 URL 开关。
- [ ] **清理足迹死代码**：`buildFootprintGrid` / `isExploredFast` / `footprintVersion` / `extraFootprints`（探索模式已不走）。
- [ ] **部分场景缺 `黄昏.png`**（美术缺口）：`坊 / 小巷 / 庭院 / 客栈一楼大厅` → 酉时回退白天图。
- [ ] **岳阳画框只含东洞庭**：西/南洞庭在框外；若要全湖需改画框重生成（瓦片量约 ×3.8）。

---

## 十三、开放待定问题（需拍板）

- **信息边界的 tag 来源**：大模型 `scene` 事件 vs 小模型异步标注（倾向后者）。
- **Pass 2 用哪个模型**：复用大模型保人物深度 vs 换更快模型。
- **活跃 → 不活跃淘汰规则**：🅿️ 搁置。
- **小游戏事件类型与暂停/恢复语义**；**音乐/战斗如何复用事件流**。
- **世界地图的数据来源**（史实骨架 + 程序化补名）。
- **`history` 何时做摘要**（等记忆系统接住关键信息后）。

---

## 十四、废弃内容生成型骰子 → 环境事件层

> 决策：不再用随机事件表"无中生有"制造内容，改由「地点定时事件 + 世界系统」涌现。
> 详细说明见 `trpg-world/废弃/README.md`。

- [x] **Step 0（2026-09-17）**：`daily_event_dice` / `travel_event_dice` 摘出 LLM 工具集
      （`registry._DISABLED`，设 `TRPG_EVENT_DICE=1` 可回滚）；规则 md 移入 `trpg-world/废弃/`（不再注入）；
      `roll_dice` 描述清理。代码保留（`tools/大模型/event_dice.py`）。
- [ ] **Step 1：环境事件层（替代物）**
  - [ ] **地点定时事件**：把 `place_notes` 升级为带**时间 / 触发条件**的结构（或复用 `宏观时间线.json`），
        由 `time_flow.pump()` 在**跨时辰 / 跨日**时检查 → 命中就注入 `pending_notes`，让 GM 演出
        （如「元宵夜戌时·三更市灯会」）。
  - [ ] **NPC 在场 / 移动进 GM 视野或地图**（Phase 2），让"人"成为事件源。
  - [ ] **养成 / 小游戏作为主动内容源**（玩家自己会去找，不需随机塞）。
- [ ] **Step 2：验收后正式删代码**——硬标准：
      **"玩家什么都不做，静置一个游戏日，世界能不能自己冒出一件有趣且合逻辑的事？"**
      能 → 删掉 `event_dice.py` 与 `_DISABLED` 条目；不能 → 先别删（或保留极简"环境噪音"层）。
- [x] ~~**`混乱度.json` 归属**~~ → **已废弃**（2026-09-18）：文件已删，系统不要了。
- [ ] **旅途骰子压到最后**：城外 / 跨城内容最稀薄，且世界地图 / 旅行机制（§8.3）未做；
      在替代覆盖旅途前，可临时用 `TRPG_EVENT_DICE=1` 回滚。

---

## 十五、十年一档 / 人物生老病死（框架决策，待拍板）

> 背景：**人会生老病死**，一档只玩约十年；玩完一档开新档。
> 正典 55 人「几乎不拘泥于时代」→ 跨档**恒存**（不衰老、不病亡），当门派的固定锚。
> 现有架构已支撑大半：时钟是标量（起始年可挪）、宏观时间线已覆盖 1220~1279、天气按 day-of-year 忽略年份。

- [ ] **开局年代**：新档可选起始年（1220/1230/…/1270）——`game_clock` 可 `set_civil`，宏观线天然分年代。
- [ ] **恒存标记**：正典 55 人档案加 `恒存: true`（不记年龄/不衰老/不病亡）；`world_sim`/`get_character`/生卒逻辑跳过。
- [ ] **非正典「在世」**：档案加 `生年`；跨年结算年龄；衰老影响属性；死亡 → 移入 `不活跃/故去` + 写入 `gm_memory`（成为史实）。
- [ ] **跨档规则**：待定（见下）。
- [ ] **天气跨年**：现按 day-of-year 查、忽略年份（年度复用）；十年档可接受，若要年代差异再议。

### 待拍板的 5 个问题
1. **梁峰**：同一人接着老 10 岁（带前档记忆/关系），还是每档新主角？
2. **非正典 NPC**：跨档延续（变老/可能已死），还是随档消失？
3. **正典动态档案**（与梁峰的关系/近记忆）：跨档保留还是清零？（取决于 1）
4. **`gm_memory`**：跨档累积当「史书」？（倾向：是）
5. **开局年代**：玩家选，还是自动接上一档？

---

## 十六、上下文最优 / 类脑结构（搁置 · 待探讨）

> 目标：让每一轮喂给大模型的上下文**全是有用的、必需的信息**。
> 搁置原因：方案未定，且“最优”缺可观测的目标函数。以下为已探讨的素材。

**已否决**：
- **两段式**（Pass1 GM + Pass2 各 NPC）：每轮 N+1 次大模型调用，**太慢太贵**。
- **按 NPC 视角严格隔离**：角色是牵线木偶，追求完美还原角色视角不对；GM 本应全知。

**已讨论备选**：
- 滑动窗口 + 实时浓缩 + 全量召回（原文保留：`current.jsonl` / `游戏存档.md` / `归档存档/` 已有，无需改）。
- **自动预检索**：不靠 GM 自觉，引擎每轮主动注入；关键词（代码）→ 本地 embedding（已有）。
- **预算制**：本局原文 ≤ 预算（如 ~40KB）→ **直接全量注入**（短局根本不会缺信息）；超限才启窗+摘要+召回。
- 手 `recall_history` 工具作兜底。

**待想清**：
1. **cue（检索线索）谁生成**：脑用“整个系统状态”当线索；我们要么代码抽实体、要么本地 embedding、要么用导演简报的活跃线。
2. **相关性 ≠ 有用性**：有用 = 会改变下一步输出的约束；预算该花在“模型推不出来”的东西上（具体事实 / 承诺 / 秘密）。
3. **分层分辨率**（工作记忆 / 近窗 / 摘要 / 仅索引）而非单一窗口。
4. **写时巩固 vs 读时召回**的配比。
5. 把事实**结构化进状态**可能比召回更可靠（永远在场、紧凑、机械）。
6. **目标函数**：如何观测“漏了 / 错了 / 冗了”（玩家纠正、GM 与日志冲突）→ **先有信号再谈优化**。

---

## 十七、交接遗留（给下一个接手的人）

> ★ **先看 `交接.md`**——本轮（2026-09-18 晚）的增量：新世界观（五行/伪神/五支组织）、
> 信息分层（GM 只看世界设定、故事大纲给导演）、工具边界修复、GM 上下文瘦约 20%。
> **53 项改动未提交 git。**

- ⚠️ **不要跑 `npm run build`，除非用户要求**（前端用 `npm run dev`；改完只跑 `npx tsc -b`）。
- ⚠️ **`tsc` / `build` 抓不到 runtime 错**：Hooks 放在 early return 之后 = 白屏（已踩两次）。
- ⚠️ 读 `README.md` 先看 §0–§5，再看 §13.5（多城市）/ §16（变更历史）与 §17（坑）。
- ⚠️ `trpg-server` 代码改动**必须重启后端**才生效（改规则 md / `时间影响.json` / `营业时间.json` 免重启）。
- ⚠️ **只跑一个 `python main.py`**；**别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`**。
- ⚠️ 地图命令**全部城市化**：单步跑脚本要带 `TRPG_CITY=<城市>`，否则默认扬州。

### 本轮（2026-09-18）做了什么（详见 README §16.16）
1. **多城市**：资产按 `map_id` 分目录（瓦片/点击层/碰撞层/空间库）；`城市.py` 单一真相源；
   `生成.py --city` 一键链路；前端 `mapId.ts` + `?map=` + 启动同步。
2. **游戏城市由玩家坐标推导**（`map_settings.get_map()`）；**看地图零副作用**；步行不可跨城（硬闸）。
3. **地图搜索切城**：`GET /search` + 侧边菜单「地图」顶部搜索框（回车跳转 + flyTo）。
4. **岳阳地图**：源改为 `湖南.pbf` 全量 → 完整洞庭湖；君山岛；`build_world` 多城市配置。
5. **锦香宫**：`岳阳_建筑群.json` + `build_compounds()` + `palace` 图标。
6. **地形步速倍率**：`tags` 列 + `terrain` 层 + `terrainAt()` + 徽章。
7. **瓦片开始画建筑**（之前 `should_draw_building` 直接 `False`）。
8. **修了一批多城市连锁 bug**（瞬移 / 白屏 / 碰撞错 / 604 公里 / 背景不换）—— 清单见 README §16.16 ⑥。

### ⚠️ git 状态
- **本轮改动未提交**。建议按主题拆几个提交：
  ① 多城市基础设施（`城市.py` / `生成.py` / `*_map_settings.py` / `mapId.ts`）
  ② 岳阳地图与锦香宫（数据 + `build_world.py` + `icongen`）
  ③ 地形步速（`create_spatial_db` / `export_walkable` / `walkable.ts` / `Map.tsx`）
  ④ 文档（README / TODO）
- `public/data` 从扁平改成 `public/data/<map_id>/`（删旧增新）；`public/tiles` 已在 gitignore。

### 依赖 / 环境
- LM Studio：`Qwen3-Embedding-0.6B`（检索）+ `qwen3-4b-2507`（小模型）。
- `.env`：`LLM_API_KEY`（DeepSeek）+ `GLM_API_KEY`（导演）。**`TRPG_MAP` 只是兑底默认，不用手动设。**
- 开关：`TRPG_WORLD_SIM=0` / `TRPG_CLOCK=0` / `TRPG_DIRECTOR=0` / `TRPG_UI_SIM=0` /
  `TRPG_EXPRESSION_SIM=0` / `TRPG_EVENT_DICE=1`（回滚骰子）/ `TRPG_FILE_TOOLS=1`。

### 下一步（按优先级）
1. **战斗时间折算接入**（⚑ 优先级 2）。
2. **地域差异化**（§8.2：城区算法仍是同一套）。
3. **环境事件层**（§十四 Step 1）；信息边界（§九）；知识库（§十）。
4. **上下文最优 / 类脑结构**（§十六，搁置待探讨）。
