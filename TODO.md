# TODO — 待办事项

## 一、战斗系统

回合制战斗。梁峰每回合可选择：**攻击 / 技能 / 防御 / 撤离**。

- 技能 = `属性.json` 里的五行技能
- 采用 **HP / TP** 机制（已有 `状态.json` 的生命值/精力值 + `modify_hp` / `modify_tp` 工具）

## 二、小游戏系统

集成小游戏，可直接在前端游玩：

- 围棋
- 象棋
- 投壶
- 赌博
- （可扩展）

## 三、背景音乐

引入背景音乐（BGM），随场景/剧情切换。

## 四、重构 LLM 返回结构（背景/音乐工具化）

- 把「背景（图）」的选择从返回结构里的 `type="bg"` 字段，改为**封装成工具调用**
- 背景音乐与背景图**同构**：都通过工具设定，前端按工具结果切换背景 / 播放音乐

## 五、属性修改 / 养成工具

- 目前 `属性.json` 只能读（`get_ability`），不能改：文件工具已禁写游戏数据目录，且无专用修改工具
- 需要新增 `update_ability` / `train_skill` 工具，服务于养成系统（练功变强、技能熟练度、招式解锁）
