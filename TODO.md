# TODO — 待办规划

> 架构决策与理由见 `ARCHITECTURE.md`；当前状态见 `STATUS.md`。
> 迁移路线（⑨ 之前的编号）见 `ARCHITECTURE.md` 第九节。

---

## ⚑ 当前优先级（交接用）

> `history` 滑动窗口/摘要：用户表示模型上下文足够大，**暂缓**（长局/成本敏感时再评估）。

1. **WASD 碰撞（可走网格）** —— 现在能穿房屋 / 河流；从空间库预烘可走栅格（`walkable.py`）→ 前端查表碰撞。
2. **战斗时间折算接入** —— `clock.begin_battle/end_battle` 已就绪，但战斗流程还没接。
3. **`/move` 边走边同步**（可选）—— 坐标现在只在输入时提交。
4. **属性养成**（`update_ability` / `train_skill`）—— 改完属性调 `derived.sync()`（见 §六）。
5. ~~战斗系统（§四）~~ → **v0.1 已完成**（见 §四；剩余 v0.2 项在该节）。
6. 大工程**未动**：**信息边界（§九）**、**知识库层（§十）**。

> 另：**大跨度时间流逝规则**（总览.md「住店≠睡到天亮」）已有 `sleep` 工具 + 规则 14，
> 但未专门补「住店只是订房」那条——规则热更，需要时补。

---

## 〇、已完成（挪出待办）

- ~~重构 LLM 返回结构（背景工具化）~~ → 已完成：`bg` 类型删除，背景改走 UI 事件 `kind:"bg"`。
- ~~存档流程重写~~ → 已完成（`save_pipeline.py` + `POST /save`）。
- ~~工具双份登记~~ → 已合并为 `tools/registry.py`。
- ~~记忆库两 collection~~ → 已完成（`gm_memory` / `char_memory`）。
- ~~世界状态 + 日推演 worker + 宏观时间线~~ → 已完成（第 ⑦ 步）。
- ~~UI 事件管线（⑧）~~ → 已完成：`bg`/`music` 由小模型选（`tools/ui_sim.py`，双清单+绑定+默认曲）。
- ~~时间加「刻」+「继续」刻数按钮~~ → 已完成（1 时辰=8 刻；0=只看信息）。
- ~~金钱请款确认 + 账目~~ → 已改为**直接落账**（`tools/money.py`；请款/确认环节与账本均已删除）。
- ~~行动 / 台词分离（`say` 模式）~~ → 已完成。
- ~~城池内外判定~~ → 已完成（`map_query.city_context`）。
- ~~规则文件热更新~~ → 已完成（`engine._rules_text`，改 `主持人/*.md` 免重启）。
- ~~动态档案为主 + LRU~~ → 已完成：存档专用工具 `update_character_archive` 写动态档案（模板 §9–§13），§10 超 20 条淘汰最旧→`char_memory`；存档不再直写 `char_memory`。
- ~~总纲第 5 条「硬事实由代码裁决」~~ → 已写入 `ARCHITECTURE.md` 总纲。
- ~~地点见闻（`place_notes`）~~ → 已完成（§8.1；`update_place_note` 存档专用，`query_nearby`/`query_place` 合并返回）。
- ~~方位注入（八方位 / 城区方位）~~ → 已完成（`map_query.bearing_name` / `city_context.城区方位` / `update_location.移动方位`）。
- ~~前端去 `data/` + 势力后端化~~ → 已完成（`trpg-world/势力介绍.json` + `GET /factions`；见 §三）。
- ~~难度设置 + 归隐退出~~ → 已完成（`tools/difficulty_settings.py` + `GET`/`POST /settings`；难度及各级含义存 `游戏数据/难度设置.json`）。
- ~~数据目录迁移 / 日志重命名~~ → `游戏数据`/`天气数据`/`归档存档` 移到 `trpg-server/`（与 `tools/` 同级）；过程日志统一 `current.jsonl`。
- ~~连续时钟 v0.2~~ → 已完成：`tools/game_clock.py`（单一「游戏秒」+ 三态 + 惰性求值 + 南宋**全年号/干支**）、
  `tools/time_flow.py`（跨时辰扣精力/熬夜 + 跨日切天气+推演 + `sleep`）、`tools/derived.py`（体力/內力→上限）、
  前端**古钟 HUD**（`Clock.tsx` + `useGameClock.ts`，rAF 插值）；`/clock` 路由；`时间影响.json` 调参。
- ~~三模式状态机（探索 / 叙事 / 战斗）~~ → 已完成：前端 `gameMode`；**探索 = 整屏 zoom18 地图 + WASD**；
  进叙事由玩家输入（带坐标）、退叙事由主持人（`resume_exploration`）；坐标为 `【移动】` 系统提醒。
- ~~宏观时间线扩充~~ → **89 条（1220~1279）** + `ensure_timeline` **增量合并**（旧存档也生效）。
- ~~修复：模拟战泄漏 / file_tools 摘除 / `state_manager` 并发写崩溃 / 背景误判乡村~~ → 均已修（见 `STATUS.md` 坑 13）。

---

## 一、世界推演收尾（⑦ 剩余）

- [x] **活跃人物名单自动化** ✅：存档管线（`save_pipeline.contacted_characters`）用三个确定性来源
      抽取本局接触过的人物（`chat` 发言人 / `get_character` 解析名 / `char_memory.owner`），
      经 `world_state.promote_active` 写入 `活跃/<名>.md`（幂等；曾淘汰者自动迁回）。
      **所有角色地位平等**：新登场的 NPC 也建档（静态 §1–§8 + 动态 §9–§13），
      一经建档即与预设角色同等（都可被 `get_character` 读、都可被推演）。
- [x] **`get_character(name)` 工具** ✅：NPC 登场时一次读全**静态正典 + 动态近记忆**
      （`tools/get_character.py`，已替代 `find_specific_character`）。**深层记忆不由它取**——
      由 LLM 按需调 `DB_query_tool(collection=char_memory, owner=…)`。
- [ ] **活跃 → 不活跃 淘汰规则**（LRU？按出场频率？）—— 🅿️ **搁置**：等活跃 NPC 真正增多、出现体积/噪音问题后再定。
- [x] **`世界状态.json` 体积与注入策略** ✅：注入时改用 `world_state.context_view()`——人物线程按小模型的
      `纳入上下文`（离玩家远近）裁剪，宏观/定时线不裁；**存储全量**（存档全量采纳）。已测：存储全量、注入裁剪。
- [x] **时间权威** ✅：规则写入 `总览.md` 通用规则 10；`engine._check_time_authority` 机械兜底
      （检出时间流逝却未调 `update_time` → 下一轮注入提醒，补上后清除）。

---

## 二、UI 事件管线（⑧）✅

- [x] **`ui_event` 的 kind 清单与协议** ✅：`bg` / `music` / `minigame`（`tools/ui_events.py` + 前端 `types/gametype.ts`）。
- [x] **把 `bg` 从大模型提示词迁到小模型** ✅：`总览.md` 删除 bg 示例并禁止输出 UI 事件；删 `主持人/可用场景.md`；
      `tools/ui_sim.py` 按叙事选场景（枚举=前端 `assets/背景/` 子目录）。
- [x] **背景音乐（BGM）** ✅：`kind:"music"`；小模型选曲（枚举=前端 `assets/音乐/*.mp3`）；前端 `in-game/music.ts` 播放/循环，离开游戏停乐。

---

## 三、内容层（⑥）

- [ ] **现有 55 份静态档案逐步对齐模板**（`档案模板/静态模板.md` §1–§8；动态内容迁往 `活跃/<名>.md`）。
- [x] **`GET /factions` 投影** ✅（2026-09-13）：玩家可见势力独立为 `trpg-world/势力介绍.json`（已剔除剧透，人工维护）；后端 `tools/factions.py` + `GET /factions`；前端画廊改 fetch（`src/data/` 已删）。
      - `trpg-world/江湖势力/*.md` 仍为 **GM 正典（含剧透）**，不经接口暴露。

---

## 四、战斗系统（⑨）✅ v0.1

**已实现**（设计见 `trpg-world/战斗系统.md`，架构见 `ARCHITECTURE.md` 第十三节）：

- **n vs n 回合制**；网格 **10×6**（切比雪夫距离）；先手方→后手方**阶段制**
- 每回合**最多 3 次模型调用**：玩家「思路」判定 1 次（小/大模型可切）+ 友方阵营 1 次 + 敌方阵营 1 次
- 玩家 **6 动作**：移动 / 舞剑（普攻不耗内力）/ 防守 / 技能（五行）/ 交流（队友或对手）/ 撤退
- **HP / TP** 复用 `状态.json`；五行相克；**武器类型**（利器×1.25+命中流血 / 钝器×1.0+会心眩晕 / 徒手×0.9）
- **Buff**：流血 / 中毒 / 内伤 / 破绽 / 眩晕 / 护体 / 士气 / 动摇 / 致盲 / 洞察 + 蓄力 / 穿甲
- **思路修正由代码映射**（模型只出「评价」枚举，±10 以内，只作用于伤害）；小模型失误一律降级
- 工具 `start_battle` + 路由 `/battle/*`；2D 俯视桌面**矩形占位** token 前端
- **环境设定「模拟战斗」**（选友军/敌人直接测）；**战斗 BGM**（boss 绑定专属曲）

**路线 B：深化为「真战棋」（v0.2+，已定方向）**

> 定位：**TRPG 主干 + 战棋战斗模块**；战斗往「真战棋」深化。

- [ ] **地形 / 障碍**：墙、桌椅、水、高低差、掩体、困难地形（影响移动/射程/视线/命中）
- [ ] **寻路 + 移动预览**：障碍连成可寻路图（Dijkstra/A*）；前端高亮可达格与路径
- [ ] **借机攻击 / 控制区（ZOC）**
- [ ] **目标多样化**：歼烲 / 护送 / 坚守 / 夺旗 / 斩首 / 存活 N 回合
- [ ] **技能体系**：NPC 按档案配 `招式`；玩家多行招式 / 融合技
- [ ] **养成 / 装备**：`update_ability` / `train_skill`；武器 / 防具 / 道具
- [ ] **更多数值维度**：暴击 / 格挡 / 护甲 / 破甲 / 命中闪避细分
- [ ] **战果写回世界状态**：伤亡 / 俘虏 / 战利品 / 名声（接 `update_money`、背包、世界推演）
- [ ] **视野 / 战争迷雾**、**先手 / 行动条**（可选）；更大 / 多层地图
- [ ] 玩家作为**受击方**时的五行判定（现只在施放方结算）；扇形 / 直线范围

---

## 五、小游戏系统（⑨）

集成小游戏，可直接在前端游玩（统一策略：**逻辑搬后端 + React 重画 UI，结果回流游戏状态 + LLM 叙事**）。

### 可复用现成实现（`C:/Users/20866/Desktop/TRPG2/tools/`）

| 文件 | 玩法 | 迁移要点 |
|------|------|---------|
| `围棋.py + gnugo.exe` | GNU Go 引擎（GTP 协议），5 档难度 | `GnuGoAI`/`Board` 类纯逻辑可搬后端，GUI 用 React Canvas 重写 |
| `投壶.py` | 力度 + 角度双阶段控制 | 状态机逻辑可搬后端 |
| `斗蟋蟀.py` | 蟋蟀自动对战 + 下注 | 最轻，直接挂 `modify_money`，**建议先做它打通链路** |
| `科举.py + 科举题库.json` | 答题 | 题库现成 |
| `生成天气.py` | — | 已迁移为本项目天气系统 |

- [ ] 象棋（无现成实现，需规则库/引擎）
- [ ] 赌博（本质是"下注模块"，可复用给斗蟋蟀）

---

## 六、属性修改 / 养成工具

- [x] **上限派生已做**（2026-09-15）：`tools/derived.py` —— **体力 = 生命值上限、內力 = 精力值上限**；
      `属性.json` 为真相源，`状态.json` 的上限由 `sync()` 对齐（`snapshot_state`/`pump` 时）。
      睡眠恢复量 = 內力 ÷ 睡眠回满时辰，养成提高內力后自动变强。
- [ ] `属性.json` 目前**只能读**（`get_ability`），文件工具已禁写游戏数据，需专用接口：
      `update_ability` / `train_skill`（练功变强、技能熟练度、招式解锁）——**做好后请调 `derived.sync()`**
      （或直接在接口里改体力/內力，让上限自动跟上）。
- [ ] **轻功作为技能**：移动速度要用轻功等级（见 §8.4 行程耗时）；需要技能体系能表示 / 升级轻功。

---

## 七、日历系统

游戏内历法：**新历（公历）＋农历并行**，作为「时间」的权威换算层。

- [x] **年号 / 干支 / 中文月日（南宋全表）** ✅（2026-09-15）：`game_clock.ERAS`（1127 建炎
      ~ 1279 祥兴，22 个年号）+ `era_name` / `ganzhi_year` / `chinese_date`；写入
      `基本信息.时间.纪年`/`干支`（LLM 可见）并进 `/clock` 锚点；前端不再自备年号表。
      **注意**：目前只是「年号 + 公历月日的中文写法」，**尚未做农历换算**（见下）。
- [ ] **双历并行的数据结构**：`基本信息.json` 的时间现已是「单一游戏秒 + 派生投影」，
      但尚未做**农历**（朔望/闰月）与公历的并行换算。
- [ ] **农历与节气**：二十四节气、传统节日（元宵/端午/中秋/除夕…）、朔望月相——
      用于市集、庙会、灯会、祭祀等定时活动（与地图 `description` 的定时事件联动）。
- [ ] **与既有系统的联动**：
      - 天气系统：按节气/季节给出更合理的天气分布；
      - 世界推演：宏观时间线按新历日期触发，需与日历统一（`世界状态.模拟游标`）；
      - 日常骰子事件 / 旅途骰子事件：按节日给不同事件权重。
- [ ] **`update_time` 升级**：支持「过 N 天 / 到某日」等表达，并自动换算农历；
      强化「时间必须权威」（见第一节）。
- [ ] **前端显示**：状态栏 / 时间面板显示「嘉定X年X月X日（农历…）· 节气 · 时辰」。
- 注：历法**不绑定真实日期**，是纯游戏内时间；只做展示与规则，不改现实时钟。

---

## 八、地图内容扩展

### 8.1 地图数据库加 `description` / `type`

- [x] **`features` 表加 `description`** ✅：`db/create_spatial_db.py` schema 加 `description`（静态说明，可为空）。
- [x] **动态见闻 `place_notes` 表** ✅（2026-09-13）：独立表（建库重建**不删**），存「某地发生过的事」；
      新工具 `update_place_note(place, note, time)`（**存档专用**，存档蒸馏时按地点写入，见 `存档流程.md`）；
      `query_nearby` / `query_place` 命中地名时把见闻合并进 `description` 返回。已播种本局事件（怀茂青楼/太阳坊）。
- [ ] **`type`（语义分类）**：区别于现有 `category`/`ancient_kind`，待做。
- [ ] **定时事件**：`description` / `place_notes` 记录「xx 广场在 xx 时刻会定时有烟火表演」这类**时空化描述**，
      与日历系统联动（如「元宵夜 戌时 三更市 灯会」）。
- [ ] **静态初值产出**：`build_world.py` 从 OSM tag / 命名池生成 `description` 初值，再由 LLM/人工补写。
- [x] **多条而非单字段** ✅：已用 `place_notes` 表（多条）而非单字段。
- [ ] **补史实城门 / 水道**：现只有 7 座城门（「水门」在城**东北**），**无「西水关」**等史实水门；
      实战中 GM 因地图无「西水关」而借把「水门」当西水关。需据史实补全（改地图源 → 重建空间库）。

### 8.2 地图特色重制（地域差异化）

- [ ] **问题**：`build_world.py` 目前是**同一套算法**，所有城市必然长得一样（坊巷棋盘 + 同种 POI 池）。
- [ ] **引入「城市风格参数 / 模板」**，按地域给不同配置：
      - 江南水乡：河网密布、桥多、沿河街市、白墙黛瓦、密度高；
      - 北方/中原：规整里坊、城墙厚重、水系少、院落方正；
      - 山地/关隘：顺应等高线、窄巷、梯田/关城；
      - 沿海/港口：码头、市舶、仓库、坡地。
- [ ] 可参数化的维度：路网形态（棋盘/自由/放射）、坊巷间距、建筑密度与朝向、
      水系与植被比例、坊名/POI 词表（方言地名）、城墙形制、色彩与瓦片样式。
- [ ] 与 `song_kinds.py` 的 zone/kind 词表结合，按地域裁剪/扩充。

### 8.3 世界地图

- [ ] **多地图支持**：`maps(map_id, name)` 已预留，但目前只有 `yangzhou` 一张。
      需要州府/路/城的**世界级地图**：位置、距离、官道/水路、旅行耗时。
- [ ] **分层缩放**：世界地图（跨城/跨区域）↔ 城市地图（城内），前端可切换/缩放；
      与 `Map.tsx` 的瓦片与点击层方案对齐（或另做一套）。
- [ ] **旅行机制**：跨图移动消耗时间与精力，接 `update_location`、旅途骰子事件、
      世界推演与日历（距离 → 天数）。
- [ ] **数据来源**：可用史实的南宋行政区划 / 驿道 / 水路作骨架，再程序化补地名。

### 8.4 地图点选目标 + 行程耗时（以「刻」为单位）

> 目标：玩家在上地图**点任意一点** → 告诉 LLM「要去这里」→ 后端算路程、算耗时（刻）→ LLM 推进时间。
> 沿总纲第 5 条【硬事实由代码裁决】：**距离 / 时间由代码算，模型只叙述**。

- [ ] **前端点选**：地图上可点击**任意坐标**标为「目的地」并发送（如 `POST /travel {lon,lat}`，或作为一次 `/action` 文本）。
- [ ] **路网寻路（后端）**：把目标点**吸附到最近的路网节点**，沿**街道网络**算实际步行路程（非直线）——
      需把 `坊巷 / 官道` 几何连成**可寻路的路网图**（Dijkstra / A*）；现有 `query_nearby` 只有几何，未成图。
- [ ] **时间以「刻」计**：路程 ÷ 速度 = 刻数，**四舍五入**取整（如 6.6 刻 → 7 刻）。1 时辰 = 8 刻（1 刻 ≈ 15 分钟）。
- [ ] **速度模型（取决于体力与轻功）**：
      - **体力**（精力值 / 饥饿 / 伤势 / 健康）→ 速度系数（低体力更慢、饥饿更慢）；
      - **轻功**（属性 / 技能等级）→ 速度系数（越高越快）；
      - 基础步行速度（米/刻）× 上述系数 = 实际速度。
- [ ] **推进时间**：后端把「目的地 / 路程(米) / 耗时(刻)」交给 LLM，LLM 调 `update_time(advance_ke=N)` 推进。
- [ ] **落库一致**：`update_location` 落到目标（或吸附后的路网点），并与行程耗时一致；
      避免「移动了时间没动 / 时间动了却没移动」。
- [ ] **与 `总览.md`「叙事节奏与移动」对齐**：现有规则按「时辰」太粗，改**以刻推进**；不足 1 刻的短距离可忽略。
- [ ] **依赖**：轻功需有技能 / 属性系统（见 §六 `update_ability` / `train_skill`）。

---

## 九、信息边界（NPC 全知 / 上下文泄漏）

> 架构决策见 `ARCHITECTURE.md` 第十节。

问题：`history` 是全知真相，一个大模型兼演 GM 与所有 NPC → NPC 说出梁峰私下所为。

- [ ] **已定方案：两段式调用**——Pass 1 GM 出叙述 + 舞台调度（不产 NPC 引语）；Pass 2 每个开口 NPC 用**过滤后的知悉集**单独出对白；合并事件流。
- [ ] **`current.jsonl` 加知情字段**：`在场` / `知悉` / `公开度`（私密·传闻·公开）/ `scene_id`。
- [ ] **知悉集拼装**：`在场∋X` ∪ `知悉∋X` ∪ 公开传闻 ∪ `char_memory[owner=X]`；`build_messages` 提供"角色视角块"。
- [ ] **tag 来源**：小模型异步标注（倾向）或大模型 `scene` 控制事件。
- [ ] **传播**：`公开度` 随 `world_sim` 日推演演化（私密→传闻→公开，沿地理/关系网扩散 `知悉`）。
- [ ] **校验**（可选）：小模型核对 NPC 台词是否引用知悉集外事实。
- [ ] 统一 `visibility` 语义：`char_memory.owner` / `turns` 知情字段 / `lore.visibility` 三层对齐。

---

## 十、知识库层（正典检索）

> 架构决策见 `ARCHITECTURE.md` 第十一节。与记忆层性质相反：md 是真相源，chroma 是可重建投影。

- [ ] 新建 `trpg-world/考据/`，整理史实/日常**事实卡片**（出处：梦粱录、武林旧事、《蒙元入侵前夜的中国日常生活》）。
- [ ] `trpg-db/chroma_lore/` + `ingest_lore.py`（独立于 `init_GM_DB --reset`；文件 hash 增量；`--reset` 重建）。
- [ ] 单 collection `lore` + metadata：`source`/`domain`/`era`/`visibility`/`confidence`。
- [ ] chunk：按标题切 + context 前缀；单块 200–500 字。
- [ ] `KB_query_tool`（只读、带出处）注册进 `registry.py`。
- [ ] 引擎每轮**预检索注入**（规则层仍全量；正典只走检索）。
- [ ] 铁律：KB 结果不进记忆；无命中必须承认、不得编造。
- [ ] 先索引现有 `trpg-world/` 正典跑通（顺带解决"正典层检索接口未做"）。

---

## 十一、其他

- [ ] **⭐ `history` 滑动窗口 / 摘要（最优先）**：长局会把大模型上下文撑爆。可先"保留最近 N 轮 + 更早摘要"；
      原则：等记忆系统确实接住关键信息后再做（避免逻辑断裂）。
- [x] ~~**待确认金钱持久化**~~ → 已去掉请款/确认环节（`modify_money` 直接落账），该条不再适用。
- [ ] **大跨度时间流逝规则**：`总览.md` 补"住店 / 投宿只是**订房**，不等于睡到天亮；过夜须玩家明说"
      （规则热更，免重启）。
- [ ] **混乱值评估自动化**：骰子会读 `混乱度.json`，但"收工时评估涨跌"未搬入，目前手动改 → 可做 `evaluate_chaos`。
- [ ] **放弃确认换成自定义浮层**（替换 `window.confirm`），与国风 UI 一致。
- [ ] **瓦片文字标注**（需中文字体）；城墙史实微调。
