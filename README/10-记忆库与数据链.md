> 本册属于 `README.md` 分册体系：**记忆库与数据链**（原 §十四、§十五）。
> 索引、工作约定、项目速览见仓库根目录 `README.md`。
> 正文里出现的 `见 §X.Y` 指 README 分册，定位见根 README 的索引表。

## 十四、记忆库

- `trpg-db/chroma_db/`：两个 collection `gm_memory` / `char_memory`（当前为空结构）。
- `init_GM_DB.py`：
  ```bash
  python init_GM_DB.py            # 建结构（不写入任何内容）
  python init_GM_DB.py --reset    # 删库重建（彻底清空）
  ```
  不需要 LM Studio。

---

## 十五、数据链总览

> 总原则：前端不存任何游戏内容——势力 / 状态 / 历史 / 前情 / 事件流一律向后端请求；
> 后端从 `trpg-world`（正典·玩家可见数据）、`游戏数据/`（实时状态）、`sessions/`（过程日志）、
> chroma（长期记忆）、`map_spatial_<map_id>.db`（空间库）取。硬事实由代码裁决。

### 15.1 势力画廊（前端零内容）

```
trpg-world/势力介绍.json          ← 玩家可见源（已剔除剧透）
      │ tools/服务/factions.py · GET /factions
      ▼
前端 GameController → AccordionGallery
（GM 正典 trpg-world/江湖势力/*.md 含剧透，不走此路）
```

### 15.2 前情回顾（进游戏前）

```
【存档时】save_pipeline ──写──► 游戏数据/游戏存档.md（逐字叙事）

【点「继续旅途」】前端 Menu ── GET /recap ──► tools/服务/recap.py
     ├─ 读 游戏数据/游戏存档.md
     ├─ 大模型 llm.complete()          ─► segments（≤10 段 narration）
     └─ 小模型 ui_sim.generate(末段, 当前地点) ─► bg / 音乐
 ◄── {has_recap, segments, bg, music}
      │ onStartGame(...)
      ▼
 App.startData ─► Gaming(initialRecap / initialBg / initialMusic)
      ▼ in game：GameScene 逐段点击推进（onFinish）→ 播完转正常游戏
```

### 15.3 游戏主循环（每回合）

```
玩家输入 POST /action {input, mode, ke, 坐标?}
   ▼ engine.TurnRunner
   ├─ 状态现拼 state.snapshot()（游戏数据/*.json，排除 足迹/时钟）
   ├─ 大模型 ↔ 工具循环（registry：游戏 31 工具；12 轮上限）
   └─ 小模型 ui_sim 选 bg / 音乐
   ▼ 统一事件流 [ ui 事件… , chat / narration… ]
前端 GameController：ui → 旁路；chat/narration → GameScene
```

### 15.4 状态 / 记忆 / 档案 / 见闻

```
读：GET /state   ◄─ state.snapshot()（游戏数据/*.json）
    GET /history ◄─ sessions/current.jsonl

存档：POST /save → save_pipeline
   ├─ 大模型蒸馏 → gm_memory（客观）
   ├─ update_character_archive → 角色动态档案 §9–§13
   │        └─ LRU：§10 超 20 条 → char_memory（冷库）
   ├─ update_place_note / update_place_structure → 空间库（见闻 / 建筑结构）
   └─ 代码：誊写 游戏存档.md + 归档 + 重置会话
```

### 15.5 模型分工一览

| 产物 | 大模型（DeepSeek） | 小模型（Qwen3-4B） | 代码 |
|---|:--:|:--:|:--:|
| 回合叙事 chat / narration | ✅ | | |
| 背景 bg / 音乐 music | | ✅ | |
| 世界推演（离散采样） | | ✅ | |
| 前情回顾文字 | ✅ | | |
| 前情回顾 bg / 音乐 | | ✅ | |
| 存档蒸馏（gm/char/档案/见闻/结构） | ✅ | | |
| 导演简报（每局一次） | GLM（异步） | | |
| 距离 / 时间 / 方位 / 骰值 / 状态 | | | ✅ |
| 战斗：思路判定（可切大模型） | ✅ | ✅ | |
| 战斗：NPC 战术（默认，L2+L3） | | | ✅ |
| 战斗：命中/伤害/HP/内力/Buff/胜负 | | | ✅ |

### 15.6 设置（难度）

```
菜单「环境设定」 ── GET/POST /settings ──► tools/服务/difficulty_settings.py ──► 游戏数据/难度设置.json
      └─（随「状态现拼」每轮注入状态块）──► 大模型按难度调整「世界如何回应」
                                             （不改硬事实：距离/时间/骰值仍由代码算）
```

### 15.7 战斗系统

```
【开战·正式】GM 判定动手
   └─ 工具 start_battle(敌人,友方,缘由) ──► battle_session.start()
         ├─ _tier_map() 读 武力排名.md（名→梯度）
         ├─ new_battle()：player_combatant() 读 状态/属性/基本信息 + 招式表 → Battle
         ├─ BattleRunner.start()：begin_round → 走 NPC（代码战术）→ 停在玩家
         └─ 选战斗 BGM：ui_sim.battle_track_for(敌人, 地点)
   ◄── UI 事件 kind:"battle"（含 战场/日志/音乐）

【一回合】POST /battle/action {动作,目标,招式,目标格,思路}
     ├─ judge_thought(...)   ← 只出「评价」→代码映射修正
     ├─ Battle.perform(玩家, ...) ← 纯代码结算 + 写日志
     └─ advance() → end_round → begin_round

【结束】battle_session._finalize()
     ├─ 玩家 生命/精力/伤势 → 状态.json（模拟战斗跳过）
     └─ 结果摘要 → session.pending_notes（模拟战斗不注入）

【读写】读：状态/属性/基本信息/招式表/武力排名/音乐表；写：状态.json
        战斗状态本身只在内存（battle_session._RUNNER）
```
