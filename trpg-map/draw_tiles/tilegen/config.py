import os


# ============================================================
# 文件配置
# ============================================================

# 本项目根目录（tilegen/ 的上一级）：
# 输入数据（map_ancient_song.json）与输出（tiles/）都放根目录
PROJECT_ROOT = os.path.dirname(
    os.path.dirname(os.path.abspath(__file__))
)

INPUT_FILE = os.path.join(
    PROJECT_ROOT,
    "map_ancient_song.json"   # 南宋转译版（含布点），瓦片与点击层同源
)

OUTPUT_DIR = os.path.join(
    PROJECT_ROOT,
    "tiles"
)

TILE_SIZE = 256

MIN_ZOOM = 11
MAX_ZOOM = 16

# 是否跳过没有任何对象的空瓦片
# 16:9 强制画框模式下建议保持 False（铺满整个矩形，边缘留白也生成）
SKIP_EMPTY_TILES = False


# ============================================================
# 自动 16:9 取景（不使用固定半径）
#
# 流程：
#   1. 收集以 CONTENT_CENTER 为中心、CONTENT_MAX_KM 以内
#      的对象（超远铁路/边界等“尾巴”不参与取景，
#      但伸进画框内的部分仍会绘制）；
#   2. 求出内容 bbox，再外扩成 16:9 的画框；
#   3. 四周再留 MAP_MARGIN_FRACTION 比例的空白。
# ============================================================

CONTENT_CENTER_LON = 119.4175
CONTENT_CENTER_LAT = 32.41
CONTENT_MAX_KM = 30.0

MAP_ASPECT_W = 16
MAP_ASPECT_H = 9

MAP_MARGIN_FRACTION = 0.03


# ============================================================
# 地图底色
# ============================================================

BACKGROUND_COLOR = (238, 231, 207)


# ============================================================
# 水
# ============================================================

WATER_COLOR = (190, 201, 190)

WATER_OUTLINE = (105, 116, 98)

RIVER_COLOR = (91, 108, 94)


# ============================================================
# 土地
# ============================================================

FARMLAND_COLOR = (222, 210, 175)

FOREST_COLOR = (194, 198, 166)

GRASS_COLOR = (216, 211, 178)

CEMETERY_COLOR = (205, 200, 177)


# ============================================================
# 建筑
# ============================================================

BUILDING_COLOR = (213, 204, 178)

BUILDING_OUTLINE = (119, 108, 88)

HISTORIC_BUILDING_COLOR = (197, 179, 143)

HISTORIC_BUILDING_OUTLINE = (102, 87, 67)

TEMPLE_COLOR = (201, 184, 150)

TEMPLE_OUTLINE = (104, 82, 58)


# ============================================================
# 城墙
# ============================================================

WALL_COLOR = (78, 60, 42)


# ============================================================
# 道路
# ============================================================

PRIMARY_COLOR = (111, 77, 55)

SECONDARY_COLOR = (126, 91, 67)

TERTIARY_COLOR = (142, 111, 83)

RESIDENTIAL_COLOR = (157, 128, 99)

OTHER_ROAD_COLOR = (170, 145, 115)


# ============================================================
# POI
# ============================================================

POINT_COLOR = (91, 68, 48)


# ============================================================
# 墨色
# ============================================================

INK_COLOR = (83, 72, 57)

LIGHT_INK = (132, 119, 94)

FAINT_INK = (170, 157, 129)


# ============================================================
# 全局 Zoom
#
# 目前保留这个变量，
# 后续可以进一步改成显式传参。
# ============================================================

CURRENT_ZOOM = 10