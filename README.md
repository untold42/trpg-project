# 梁峰传（暂名）· 项目 README

明确用户需求，避免代码臃肿，不要添加不必要的复杂度。
先确认好用户的需求，再动手。

> 一个以地图为主界面、由连续时钟驱动的武侠叙事游戏：
> 大模型当主持人（GM），小模型管 UI 与世界推演，硬事实由代码裁决。
>
> 本文是项目入口：**工作约定 + 项目速览 + 分册索引**。
> 架构决策（为什么这么做）、当前状态、连续时钟 v0.2 与三模式规格、地图性能定案等
> 正文已拆进 `README/`（14 册），看下方索引。
> 待办规划与开放问题 → `TODO.md`。
>
> 原始 `README.md` + `ARCHITECTURE.md` + `STATUS.md` + `交接-地图性能.md` 曾合并成一份
> 155 KB 的单文件，2026-09-22 再按主题拆开（旧版本可在 git 历史里查到）。

最后更新：2026-09-21 晚（机制硬化 + 养成重构 + 天气/驳回 + 缓存优化）——
五行改为**一次性授予**、之后可练到极致（以 `主持人/世界.md` 为准）；养成改为**玩家自选时长 + 收益递减 + 一天一个属性一次**；
设施耗时 / 意外 / 天气 / 位置标签等改由**代码裁决**；新增**驳回上轮**；上下文缓存命中上限 73% → ~88%；
探索模式新增天气动效。详见 §16.21 与 `交接.md`。

> 更早两轮：2026-09-18 世界观（五行 / 伪神 / 五支）与 多城市 + 岳阳地图（见 §16.16）。

---

## ⚠️ 工作约定（给下一个 AI / 协作者）

> 1. 不要跑 `npm run build`，除非用户明确要求。
>
> 前端用 `npm run dev`（vite dev server，自带 HMR）。`npm run build` 比 `tsc` 多抓不到什么东西
> （模块解析错 dev server 会立刻报），但它会把 `public/tiles`、`assets/人物`、`assets/音乐`、
> 两套背景图等**几百 MB 静态资源整个复制进 `dist/`**：实测一次 build 约 2 分钟，几乎全耗在
> `prepare-out-dir` 与 800+ 次 asset load 上，而产物根本用不上。所以**默认不 build**。
>
> 改完 TS/TSX 只跑 `npx tsc -b` 做类型检查即可（几秒）；要看效果让用户开 dev server。
>
> 2. `tsc` 抓不到运行时错。 典型：Hooks 放在 early return 之后 → 白屏
> （`Rendered more hooks than during the previous render`）——`tsc` / `build` 都是绿的。
> 所以改 hook 顺序后必须硬刷新（Ctrl+Shift+R）并实际看一眼。见 §13.4 血泪坑。
>
> 3. 后端改代码必须重启（改 `主持人/*.md`、`时间影响.json`、`营业时间.json` 免重启）。
>
> 4. 只跑一个 `python main.py`；别用外部工具开着 `游戏数据/*.json` 或 `chroma_db`。
>
> 5. 地图改了必须告诉用户去哪看：多城市已分目录，不要再说「public/tiles」这种扁平路径。
>
> 6. 禁止从项目根做全盘扫描（`find .`、`grep -r .`、`du -sh .`、`sort` 全量）：仓库里有
> `node_modules/`、两万多张瓦片、存档与备份，遍历会卡几分钟。查改动一律用 `git status` /
> `git diff --stat`；查文件用已知路径的 `ls -l`；确需递归就先限定目录并排除
> `node_modules/`、`tiles/`、`归档存档/`、`__pycache__/`。任何可能耗时的命令先告知用户再跑。

---

## 零、项目速览

| 项 | 内容 |
|---|---|
| 类型 | 单人 AI 跑团 / 叙事驱动 CRPG（TRPG 输入 × CRPG 裁决 × 活世界模拟） |
| 时代 | 南宋嘉定年间 |
| 地图 | 多城市：扬州（`yangzhou`）/ 岳阳（`yueyang`）；资产按城市分目录，见 §13.5 |
| 玩家 | 梁峰（第三人称，WASD 操控） |
| 大模型 | DeepSeek（叙事 / 蒸馏 / 前情回顾） |
| 小模型 | 本地 Qwen3-4B（UI 事件 / 世界推演 / 战斗选曲） |
| 记忆 | chroma 两库 `gm_memory`（客观）/ `char_memory`（主观，带 owner） |
| 核心原则 | 单一真相源 · 状态现拼 · 信息边界 · 硬事实由代码裁决 |

一句话：把游戏从"聊天室"升级成"能走进去的世界"——地图是主界面，时间是连续流动的，LLM 只在关键时刻登场。

---

## 📑 分册索引

正文已按主题拆进 `README/`。**本文件只留：工作约定 + 项目速览 + 本索引。**

全文里出现的 `见 §X.Y` 一律按本表定位（代码注释、`TODO.md`、`交接.md` 里的引用同理）。

| 节 | 内容 | 文件 |
|---|---|---|
| §0 | 项目速览 | 本文件 |
| §1、§4 | 核心原则（总纲） / 架构总览 | `README/01-核心原则与架构.md` |
| §2 | 文档导航 | 本索引（下方「其它文档」） |
| §3 | 代码结构与常用命令 | `README/02-代码结构与常用命令.md` |
| **§5.1–§5.9** | **分层详解**（状态 / 记忆 / 内容 / 引擎 / 表现 / 小模型 / 信息边界 / 知识库 / 导演） | `README/03-分层详解.md` |
| §6 | 连续时钟 v0.2（核心） | `README/04-连续时钟.md` |
| §7 | 三模式状态机（探索 / 叙事 / 战斗） | `README/05-三模式状态机.md` |
| §8、§9 | 地图为主 UI + WASD 探索 / 古钟 HUD | `README/06-地图UI与古钟HUD.md` |
| §10 | 战斗系统 | `README/07-战斗系统.md` |
| §11、§12 | 接口（路由 / 工具 / 事件信封） / 数据与存档 · 世界推演 | `README/08-接口与数据存档.md` |
| §13 | 前端 / 世界观文档 / 地图 | `README/09-前端与内容资源.md` |
| §14、§15 | 记忆库 / 数据链总览 | `README/10-记忆库与数据链.md` |
| **§16.x** | **实现记录（变更历史）** | `README/11-实现记录.md` |
| §17.x | 已知问题 / 坑 | `README/12-已知问题与坑.md` |
| §18 | 关键文件速查 | `README/13-关键文件速查.md` |
| §19 | 路线图（MVP 边界 / Phase 2-3 / 验收） | `README/14-路线图.md` |

### 其它文档

| 文档 | 作用 |
|---|---|
| `交接.md` | 本次会话增量交接 |
| `TODO.md` | 待办规划 + 开放问题 + 交接遗留 |
| `trpg-map/draw_tiles/STATUS.md` | 地图生成细节 |
| `trpg-map/数据/README.md` | 地图数据血缘与命名约定 |
| `trpg-world/` | 世界观正典（主持人规则 / 势力 / 角色 / 世界推演 / 存档流程 / 战斗系统） |
| `trpg-client/scripts/background-pipeline/README.md` | 背景图 AI 重构素材包流程 |
| `trpg-server/导出策划数值表.py` | 一键导出 `trpg-world/策划数值总表.xlsx` |

### 本文件的来历

原 `README.md` + `ARCHITECTURE.md` + `STATUS.md` + `交接-地图性能.md` 曾合并成一份 155 KB 的单文件；
2026-09-22 再按主题拆进 `README/`（14 册），本文件留作入口。旧版本可在 git 历史里查。

