trpg-client/dist
trpg-client/node_modules
trpg-client/src/assets
trpg-client/public/tiles

# 背景 AI 重构：参考图素材包 + 已完成的包
# 两者都是硬链接到旧背景图（每张 2~3MB），可一键重跑生成，绝不入库
trpg-client/scripts/background-pipeline/packs/
trpg-client/scripts/background-pipeline/已完成/
trpg-client/scripts/background-pipeline/重画/

.env

trpg-map/draw_tiles/tiles
# 空间库：**几秒就能从 <城市>_南宋世界.json 重建**（create_spatial_db.py），
# 一城市 8~80 MB，不入库；入库的只有前端真正要用的 public/data/<map_id>/*.geojson
trpg-map/draw_tiles/db/map_spatial*.db
trpg-map/draw_tiles/_preview/
trpg-map/draw_tiles/_*.txt
trpg-map/_*.py
trpg-map/_*.txt
trpg-map/_tmp_*
trpg-server/sessions/

# 诊断脚本的一次性输出（可重跑生成，不入库）
trpg-server/开发/director_probe_out.md

# 地图数据：中间产物不入库（源 pbf 与成品 *_OSM全量/*_OSM精简/*_南宋世界 仍在库）
trpg-map/数据/中间产物/

# Python 字节码（不属于源码）
__pycache__/
*.py[cod]

# 删档备份（含证据，不入库）
trpg-server/删档备份/

# 画作：运行时生成的图（每张 2~3MB，可重画，不入库）
trpg-server/画作/
